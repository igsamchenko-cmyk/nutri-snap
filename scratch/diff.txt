diff --git a/src/App.jsx b/src/App.jsx
index 277551e..cf9928c 100644
--- a/src/App.jsx
+++ b/src/App.jsx
@@ -77,6 +77,17 @@ const getDashboardTitle = (dateStr) => {
   return `Огляд за ${formatDateLabel(dateStr)}`;
 };
 
+const ACTIVITIES = [
+  { name: 'Ходьба середня', met: 3.5, emoji: '🚶' },
+  { name: 'Біг помірний', met: 8.0, emoji: '🏃' },
+  { name: 'Їзда на велосипеді', met: 6.0, emoji: '🚴' },
+  { name: 'Плавання', met: 6.0, emoji: '🏊' },
+  { name: 'Силове тренування', met: 5.0, emoji: '🏋️' },
+  { name: 'Йога', met: 2.5, emoji: '🧘' },
+  { name: 'Футбол', met: 7.0, emoji: '⚽' },
+  { name: 'Фітнес', met: 5.5, emoji: '💃' }
+];
+
 export default function App() {
   // --- Global States ---
   const [activeTab, setActiveTab] = useState('dashboard');
@@ -132,6 +143,7 @@ export default function App() {
     return {
       weight: 70,
       height: 170,
+      targetWeight: 65,
       goal: 'maintain', // lose | maintain | gain
       targetCalories: 2000,
       targetProtein: 150,
@@ -140,6 +152,30 @@ export default function App() {
     };
   });
 
+  const [tempProfile, setTempProfile] = useState({
+    weight: profile.weight.toString(),
+    height: profile.height.toString(),
+    targetWeight: (profile.targetWeight || 65).toString(),
+    goal: profile.goal,
+    targetCalories: profile.targetCalories.toString(),
+    targetProtein: profile.targetProtein.toString(),
+    targetFat: profile.targetFat.toString(),
+    targetCarbs: profile.targetCarbs.toString()
+  });
+
+  useEffect(() => {
+    setTempProfile({
+      weight: profile.weight.toString(),
+      height: profile.height.toString(),
+      targetWeight: (profile.targetWeight || 65).toString(),
+      goal: profile.goal,
+      targetCalories: profile.targetCalories.toString(),
+      targetProtein: profile.targetProtein.toString(),
+      targetFat: profile.targetFat.toString(),
+      targetCarbs: profile.targetCarbs.toString()
+    });
+  }, [profile]);
+
   // Налаштування ШІ
   const [apiKey, setApiKey] = useState(() => {
     try {
@@ -352,6 +388,106 @@ export default function App() {
   const [calendarDate, setCalendarDate] = useState(new Date());
   const [isCalendarExpanded, setIsCalendarExpanded] = useState(true);
 
+  // --- States for Custom Barcodes, Recipes, Weight History, Exercise Logs ---
+  const [customBarcodes, setCustomBarcodes] = useState(() => {
+    try {
+      const saved = localStorage.getItem('nutrisnap_custom_barcodes');
+      return saved ? JSON.parse(saved) : {};
+    } catch (e) {
+      console.error("Error reading nutrisnap_custom_barcodes:", e);
+      return {};
+    }
+  });
+
+  const [barcodeNotFound, setBarcodeNotFound] = useState(null); // { barcode, errorMsg } or null
+
+  const [customFoods, setCustomFoods] = useState(() => {
+    try {
+      const saved = localStorage.getItem('nutrisnap_custom_foods');
+      return saved ? JSON.parse(saved) : [];
+    } catch (e) {
+      console.error("Error reading nutrisnap_custom_foods:", e);
+      return [];
+    }
+  });
+
+  const [isCustomFoodModalOpen, setIsCustomFoodModalOpen] = useState(false);
+  const [customFoodName, setCustomFoodName] = useState('');
+  const [customFoodCalories, setCustomFoodCalories] = useState('0');
+  const [customFoodProtein, setCustomFoodProtein] = useState('0');
+  const [customFoodFat, setCustomFoodFat] = useState('0');
+  const [customFoodCarbs, setCustomFoodCarbs] = useState('0');
+  const [customFoodWeight, setCustomFoodWeight] = useState('100');
+
+  const [customRecipes, setCustomRecipes] = useState(() => {
+    try {
+      const saved = localStorage.getItem('nutrisnap_custom_recipes');
+      return saved ? JSON.parse(saved) : [];
+    } catch (e) {
+      console.error("Error reading nutrisnap_custom_recipes:", e);
+      return [];
+    }
+  });
+
+  const [weightHistory, setWeightHistory] = useState(() => {
+    try {
+      const saved = localStorage.getItem('nutrisnap_weighthistory');
+      // Default weight history values for a beautiful starting chart
+      return saved ? JSON.parse(saved) : [
+        { date: getTodayString(new Date(Date.now() - 6 * 86400000)), weight: 72.5 },
+        { date: getTodayString(new Date(Date.now() - 5 * 86400000)), weight: 72.1 },
+        { date: getTodayString(new Date(Date.now() - 4 * 86400000)), weight: 71.8 },
+        { date: getTodayString(new Date(Date.now() - 3 * 86400000)), weight: 71.9 },
+        { date: getTodayString(new Date(Date.now() - 2 * 86400000)), weight: 71.4 },
+        { date: getTodayString(new Date(Date.now() - 1 * 86400000)), weight: 70.8 },
+        { date: getTodayString(new Date()), weight: 70.0 }
+      ];
+    } catch (e) {
+      console.error("Error reading nutrisnap_weighthistory:", e);
+      return [];
+    }
+  });
+
+  const [exerciseLogs, setExerciseLogs] = useState(() => {
+    try {
+      const saved = localStorage.getItem('nutrisnap_exercises');
+      return saved ? JSON.parse(saved) : [];
+    } catch (e) {
+      console.error("Error reading nutrisnap_exercises:", e);
+      return [];
+    }
+  });
+
+  // Modal / form states for new trackers
+  const [isWeightModalOpen, setIsWeightModalOpen] = useState(false);
+  const [weightInputVal, setWeightInputVal] = useState('');
+
+  const [isExerciseModalOpen, setIsExerciseModalOpen] = useState(false);
+  const [exerciseSearchQuery, setExerciseSearchQuery] = useState('');
+  const [selectedExercise, setSelectedExercise] = useState(null);
+  const [exerciseDuration, setExerciseDuration] = useState('30');
+  const [isCustomExercise, setIsCustomExercise] = useState(false);
+  const [customExerciseName, setCustomExerciseName] = useState('');
+  const [customExerciseCalories, setCustomExerciseCalories] = useState('150');
+
+  const [isRecipeCreatorOpen, setIsRecipeCreatorOpen] = useState(false);
+  const [recipeName, setRecipeName] = useState('');
+  const [recipeIngredients, setRecipeIngredients] = useState([]); // [{ food, rawWeight }]
+  const [recipeCookedWeight, setRecipeCookedWeight] = useState('');
+  const [recipeIngredientWeight, setRecipeIngredientWeight] = useState('100');
+  const [recipeSelectedFood, setRecipeSelectedFood] = useState(null);
+  const [isRecipeSearchActive, setIsRecipeSearchActive] = useState(false);
+  const [recipeSearchQuery, setRecipeSearchQuery] = useState('');
+
+  const [isCustomBarcodeModalOpen, setIsCustomBarcodeModalOpen] = useState(false);
+  const [customBarcodeVal, setCustomBarcodeVal] = useState('');
+  const [customBarcodeName, setCustomBarcodeName] = useState('');
+  const [customBarcodeCalories, setCustomBarcodeCalories] = useState('0');
+  const [customBarcodeProtein, setCustomBarcodeProtein] = useState('0');
+  const [customBarcodeFat, setCustomBarcodeFat] = useState('0');
+  const [customBarcodeCarbs, setCustomBarcodeCarbs] = useState('0');
+  const [customBarcodeWeight, setCustomBarcodeWeight] = useState('100');
+
   const getDefaultCategory = () => {
     const currentHour = new Date().getHours();
     if (currentHour >= 6 && currentHour < 11) return 'Сніданок';
@@ -457,6 +593,26 @@ export default function App() {
     }
   }, [theme]);
 
+  useEffect(() => {
+    localStorage.setItem('nutrisnap_custom_barcodes', JSON.stringify(customBarcodes));
+  }, [customBarcodes]);
+
+  useEffect(() => {
+    localStorage.setItem('nutrisnap_custom_recipes', JSON.stringify(customRecipes));
+  }, [customRecipes]);
+
+  useEffect(() => {
+    localStorage.setItem('nutrisnap_custom_foods', JSON.stringify(customFoods));
+  }, [customFoods]);
+
+  useEffect(() => {
+    localStorage.setItem('nutrisnap_weighthistory', JSON.stringify(weightHistory));
+  }, [weightHistory]);
+
+  useEffect(() => {
+    localStorage.setItem('nutrisnap_exercises', JSON.stringify(exerciseLogs));
+  }, [exerciseLogs]);
+
   // Захист від фантомних натискань (ghost click protection) при відкритті сканера або зміні режимів
   useEffect(() => {
     if (activeTab === 'scanner') {
@@ -815,12 +971,23 @@ export default function App() {
     setActiveTab('dashboard');
   };
 
+  // Спроба отримати продукт з локальної бази кастомних штрих-кодів або з Open Food Facts
+  const resolveBarcodeProduct = async (barcodeVal) => {
+    if (customBarcodes && customBarcodes[barcodeVal]) {
+      console.log("Знайдено продукт у локальній базі штрих-кодів:", customBarcodes[barcodeVal]);
+      return customBarcodes[barcodeVal];
+    }
+    return await getProductByBarcode(barcodeVal);
+  };
+
   // Запуск аналізу штрих-коду ШІ
   const triggerBarcodeScan = async (imageDataBase64) => {
     setIsBarcodeScanning(true);
     setBarcodeError(null);
     setBarcodeResult(null);
+    setBarcodeNotFound(null);
     setBarcodeMealCategory(preselectedCategory || getDefaultCategory());
+    let detectedBarcode = null;
     try {
       if (!apiKey || apiKey.trim() === '') {
         throw new Error("Будь ласка, введіть власний безкоштовний Gemini API-ключ у налаштуваннях профілю.");
@@ -831,10 +998,11 @@ export default function App() {
         throw new Error("Не вдалося розпізнати штрих-код на фото. Спробуйте інший ракурс або введіть його вручну.");
       }
 
+      detectedBarcode = barcodeVal;
       setBarcodeInput(barcodeVal);
-      
       setBarcodeLoading(true);
-      const product = await getProductByBarcode(barcodeVal);
+      
+      const product = await resolveBarcodeProduct(barcodeVal);
       setBarcodeResult(product);
       const w = product.weight || 100;
       setBarcodeEditedWeight(w);
@@ -846,6 +1014,12 @@ export default function App() {
     } catch (err) {
       console.error(err);
       setBarcodeError(err.message || "Помилка при зчитуванні штрих-коду.");
+      if (detectedBarcode) {
+        setBarcodeNotFound({
+          barcode: detectedBarcode,
+          errorMsg: err.message || "Продукт не знайдено в базі даних продуктів."
+        });
+      }
     } finally {
       setIsBarcodeScanning(false);
       setBarcodeLoading(false);
@@ -880,10 +1054,11 @@ export default function App() {
     setBarcodeLoading(true);
     setBarcodeError(null);
     setBarcodeResult(null);
+    setBarcodeNotFound(null);
     setBarcodeMealCategory(preselectedCategory || getDefaultCategory());
     
     try {
-      const product = await getProductByBarcode(barcodeVal);
+      const product = await resolveBarcodeProduct(barcodeVal);
       setBarcodeResult(product);
       const w = product.weight || 100;
       setBarcodeEditedWeight(w);
@@ -901,6 +1076,10 @@ export default function App() {
     } catch (err) {
       console.error("Direct barcode search error:", err);
       setBarcodeError(err.message || "Не вдалося знайти товар за цим штрих-кодом.");
+      setBarcodeNotFound({
+        barcode: barcodeVal,
+        errorMsg: err.message || "Продукт не знайдено в базі даних продуктів."
+      });
     } finally {
       setBarcodeLoading(false);
     }
@@ -1049,7 +1228,22 @@ export default function App() {
     const fVal = f === '' ? 0 : (parseFloat(f) || 0);
     const cVal = c === '' ? 0 : (parseFloat(c) || 0);
     
-    setBarcodeScannedCalories(Math.round(pVal * 4 + fVal * 9 + cVal * 4));
+    const calculatedCals = Math.round(pVal * 4 + fVal * 9 + cVal * 4);
+    setBarcodeScannedCalories(calculatedCals);
+
+    // Update baseline in barcodeResult to prevent resetting on weight changes
+    const currentWeightVal = Number(barcodeEditedWeight) || 100;
+    const factor = currentWeightVal > 0 ? (100 / currentWeightVal) : 1;
+    setBarcodeResult(prev => {
+      if (!prev) return null;
+      return {
+        ...prev,
+        protein: Math.round((pVal * factor) * 10) / 10,
+        fat: Math.round((fVal * factor) * 10) / 10,
+        carbs: Math.round((cVal * factor) * 10) / 10,
+        calories: Math.round(calculatedCals * factor)
+      };
+    });
   };
 
   // Обробник ручної зміни ваги порції для штрих-коду з масштабуванням КБЖВ
@@ -1198,7 +1392,14 @@ export default function App() {
     }
   };
 
-  const filteredSearchFoods = mockFoods.filter(food => {
+  const combinedFoods = [
+    ...customRecipes.map(r => ({ ...r, isCustomRecipe: true, icon: '🥘', id: r.id || `recipe-${r.name}` })),
+    ...customFoods.map(cf => ({ ...cf, isCustomFood: true, icon: '🏷️', id: cf.id || `custom-${cf.name}` })),
+    ...Object.values(customBarcodes).map(cb => ({ ...cb, isCustomFood: true, icon: '🏷️', id: cb.id || `barcode-${cb.barcode || Math.random()}` })),
+    ...mockFoods
+  ];
+
+  const filteredSearchFoods = combinedFoods.filter(food => {
     const matchesQuery = food.name.toLowerCase().includes(searchQuery.toLowerCase()) || 
                          (food.brand && food.brand.toLowerCase().includes(searchQuery.toLowerCase()));
     if (!matchesQuery) return false;
@@ -1231,7 +1432,7 @@ export default function App() {
       return isSupermarket;
     }
     if (selectedCategoryFilter === 'Страви') {
-      return food.brand === 'Українська кухня' || food.brand === 'Популярне';
+      return food.isCustomRecipe || food.brand === 'Українська кухня' || food.brand === 'Популярне';
     }
     if (selectedCategoryFilter === 'Сніданок') return food.category === 'Сніданок';
     if (selectedCategoryFilter === 'Обід') return food.category === 'Обід';
@@ -1348,6 +1549,11 @@ export default function App() {
   totals.fat = Math.round(totals.fat * 10) / 10;
   totals.carbs = Math.round(totals.carbs * 10) / 10;
 
+  // Розрахунок спорт-активностей за сьогодні
+  const currentDayExercises = exerciseLogs.filter(e => e.date === selectedDate);
+  const totalBurnedCalories = currentDayExercises.reduce((sum, e) => sum + (e.burnedCalories || 0), 0);
+  const remainingCalories = profile.targetCalories - totals.calories + totalBurnedCalories;
+
   const currentWater = waterIntake[selectedDate] || 0;
 
   const handleWaterAdd = (amount = 250) => {
@@ -1365,6 +1571,19 @@ export default function App() {
   const handleProfileChange = (key, value) => {
     const updated = { ...profile, [key]: value };
     
+    if (key === 'weight') {
+      const todayStr = getTodayString();
+      const weightVal = Number(value) || 70;
+      setWeightHistory(prev => {
+        const exists = prev.some(item => item.date === todayStr);
+        if (exists) {
+          return prev.map(item => item.date === todayStr ? { ...item, weight: weightVal } : item);
+        } else {
+          return [...prev, { date: todayStr, weight: weightVal }];
+        }
+      });
+    }
+
     // Перерахунок КБЖВ за замовчуванням при зміні цілі або ваги
     if (key === 'goal' || key === 'weight') {
       let baseCals = 2000;
@@ -1392,6 +1611,256 @@ export default function App() {
     setProfile(updated);
   };
 
+  const handleSaveCustomBarcode = () => {
+    if (!customBarcodeName.trim()) {
+      showToast("Будь ласка, введіть назву продукту", "error");
+      return;
+    }
+    const barcode = customBarcodeVal.trim();
+    const newProduct = {
+      barcode: barcode,
+      name: customBarcodeName.trim(),
+      calories: Math.round(Number(customBarcodeCalories)) || 0,
+      protein: Math.round(Number(customBarcodeProtein) * 10) / 10 || 0,
+      fat: Math.round(Number(customBarcodeFat) * 10) / 10 || 0,
+      carbs: Math.round(Number(customBarcodeCarbs) * 10) / 10 || 0,
+      weight: Math.round(Number(customBarcodeWeight)) || 100,
+      ingredients: "Додано користувачем вручну",
+      image: null
+    };
+
+    setCustomBarcodes(prev => ({
+      ...prev,
+      [barcode]: newProduct
+    }));
+
+    setBarcodeResult(newProduct);
+    const w = newProduct.weight;
+    setBarcodeEditedWeight(w);
+    const scale = w / 100;
+    setBarcodeScannedProtein(Math.round((newProduct.protein || 0) * scale * 10) / 10);
+    setBarcodeScannedFat(Math.round((newProduct.fat || 0) * scale * 10) / 10);
+    setBarcodeScannedCarbs(Math.round((newProduct.carbs || 0) * scale * 10) / 10);
+    setBarcodeScannedCalories(Math.round((newProduct.calories || 0) * scale));
+
+    setIsCustomBarcodeModalOpen(false);
+    showToast(`Продукт "${customBarcodeName}" збережено до бази!`, "success");
+  };
+
+  const handleSaveCustomFood = () => {
+    if (!customFoodName.trim()) {
+      showToast("Будь ласка, введіть назву продукту", "error");
+      return;
+    }
+    const newFood = {
+      id: `custom-food-${Date.now()}`,
+      name: customFoodName.trim(),
+      calories: Math.round(Number(customFoodCalories)) || 0,
+      protein: Math.round(Number(customFoodProtein) * 10) / 10 || 0,
+      fat: Math.round(Number(customFoodFat) * 10) / 10 || 0,
+      carbs: Math.round(Number(customFoodCarbs) * 10) / 10 || 0,
+      weight: Math.round(Number(customFoodWeight)) || 100,
+      brand: 'Власний продукт',
+      icon: '🏷️',
+      isCustomFood: true
+    };
+
+    setCustomFoods(prev => [newFood, ...prev]);
+    setIsCustomFoodModalOpen(false);
+    showToast(`Продукт "${customFoodName}" збережено до бази!`, "success");
+
+    // Open detail popup to add to diary directly
+    setSelectedSearchFood(newFood);
+    setSearchFoodWeight(newFood.weight);
+  };
+
+  const handleAddRecipeIngredient = (food, weightVal) => {
+    const weight = parseFloat(weightVal) || 100;
+    setRecipeIngredients(prev => [...prev, { food, rawWeight: weight }]);
+    setRecipeSelectedFood(null);
+    setRecipeSearchQuery('');
+    setRecipeIngredientWeight('100');
+  };
+
+  const handleRemoveRecipeIngredient = (index) => {
+    setRecipeIngredients(prev => prev.filter((_, i) => i !== index));
+  };
+
+  const handleSaveRecipe = () => {
+    if (!recipeName.trim()) {
+      showToast("Введіть назву страви", "error");
+      return;
+    }
+    if (recipeIngredients.length === 0) {
+      showToast("Додайте хоча б один інгредієнт", "error");
+      return;
+    }
+    
+    const totalRawWeight = recipeIngredients.reduce((sum, item) => sum + item.rawWeight, 0);
+    const totalRawCalories = recipeIngredients.reduce((sum, item) => sum + (item.food.calories * item.rawWeight / 100), 0);
+    const totalRawProtein = recipeIngredients.reduce((sum, item) => sum + (item.food.protein * item.rawWeight / 100), 0);
+    const totalRawFat = recipeIngredients.reduce((sum, item) => sum + (item.food.fat * item.rawWeight / 100), 0);
+    const totalRawCarbs = recipeIngredients.reduce((sum, item) => sum + (item.food.carbs * item.rawWeight / 100), 0);
+    
+    const cookedWeight = parseFloat(recipeCookedWeight) || totalRawWeight;
+    
+    if (cookedWeight <= 0) {
+      showToast("Вага готової страви має бути більшою за нуль", "error");
+      return;
+    }
+    
+    const cookedCalories = Math.round((totalRawCalories / cookedWeight) * 100);
+    const cookedProtein = Math.round((totalRawProtein / cookedWeight) * 100 * 10) / 10;
+    const cookedFat = Math.round((totalRawFat / cookedWeight) * 100 * 10) / 10;
+    const cookedCarbs = Math.round((totalRawCarbs / cookedWeight) * 100 * 10) / 10;
+    
+    const ingredientsSummary = recipeIngredients.map(item => `${item.food.name} (${item.rawWeight}г)`).join(', ');
+    
+    const newRecipe = {
+      id: `recipe-${Date.now()}`,
+      name: recipeName.trim(),
+      calories: cookedCalories,
+      protein: cookedProtein,
+      fat: cookedFat,
+      carbs: cookedCarbs,
+      weight: 100,
+      icon: '🥘',
+      ingredients: ingredientsSummary,
+      isCustomRecipe: true
+    };
+    
+    setCustomRecipes(prev => [...prev, newRecipe]);
+    showToast(`Рецепт "${recipeName.trim()}" збережено!`, 'success');
+    
+    setRecipeName('');
+    setRecipeIngredients([]);
+    setRecipeCookedWeight('');
+    setRecipeSearchQuery('');
+    setRecipeSelectedFood(null);
+    setIsRecipeCreatorOpen(false);
+  };
+
+  const handleTempProfileChange = (key, value) => {
+    setTempProfile(prev => {
+      const updated = { ...prev, [key]: value };
+      
+      if (key === 'goal' || key === 'weight') {
+        const weightNum = parseFloat(updated.weight) || 0;
+        if (weightNum > 0) {
+          let baseCals = 2000;
+          if (updated.goal === 'lose') {
+            baseCals = Math.round(weightNum * 24);
+          } else if (updated.goal === 'gain') {
+            baseCals = Math.round(weightNum * 35);
+          } else {
+            baseCals = Math.round(weightNum * 30);
+          }
+          updated.targetCalories = baseCals.toString();
+          updated.targetProtein = Math.round(baseCals * 0.3 / 4).toString();
+          updated.targetFat = Math.round(baseCals * 0.25 / 9).toString();
+          updated.targetCarbs = Math.round(baseCals * 0.45 / 4).toString();
+        }
+      }
+      return updated;
+    });
+  };
+
+  const handleTempCaloriesChange = (value) => {
+    setTempProfile(prev => {
+      const cals = Math.max(0, parseFloat(value) || 0);
+      return {
+        ...prev,
+        targetCalories: value,
+        targetProtein: Math.round(cals * 0.3 / 4).toString(),
+        targetFat: Math.round(cals * 0.25 / 9).toString(),
+        targetCarbs: Math.round(cals * 0.45 / 4).toString()
+      };
+    });
+  };
+
+  const handleTempProteinChange = (value) => {
+    setTempProfile(prev => {
+      const p = Math.max(0, parseFloat(value) || 0);
+      const cals = parseFloat(prev.targetCalories) || 2000;
+      const f = parseFloat(prev.targetFat) || 55;
+      const c = Math.max(0, Math.round((cals - p * 4 - f * 9) / 4));
+      return {
+        ...prev,
+        targetProtein: value,
+        targetCarbs: c.toString()
+      };
+    });
+  };
+
+  const handleTempFatChange = (value) => {
+    setTempProfile(prev => {
+      const f = Math.max(0, parseFloat(value) || 0);
+      const cals = parseFloat(prev.targetCalories) || 2000;
+      const p = parseFloat(prev.targetProtein) || 150;
+      const c = Math.max(0, Math.round((cals - p * 4 - f * 9) / 4));
+      return {
+        ...prev,
+        targetFat: value,
+        targetCarbs: c.toString()
+      };
+    });
+  };
+
+  const handleTempCarbsChange = (value) => {
+    setTempProfile(prev => {
+      const c = Math.max(0, parseFloat(value) || 0);
+      const cals = parseFloat(prev.targetCalories) || 2000;
+      const p = parseFloat(prev.targetProtein) || 150;
+      const f = Math.max(0, Math.round((cals - p * 4 - c * 4) / 9));
+      return {
+        ...prev,
+        targetCarbs: value,
+        targetFat: f.toString()
+      };
+    });
+  };
+
+  const handleSaveProfileSettings = () => {
+    const weightVal = parseFloat(tempProfile.weight) || 0;
+    const heightVal = parseFloat(tempProfile.height) || 0;
+    const targetWeightVal = parseFloat(tempProfile.targetWeight) || 0;
+    const targetCalsVal = parseFloat(tempProfile.targetCalories) || 0;
+    const targetProteinVal = parseFloat(tempProfile.targetProtein) || 0;
+    const targetFatVal = parseFloat(tempProfile.targetFat) || 0;
+    const targetCarbsVal = parseFloat(tempProfile.targetCarbs) || 0;
+
+    if (weightVal <= 0 || heightVal <= 0 || targetWeightVal <= 0 || targetCalsVal <= 0) {
+      showToast("Будь ласка, введіть коректні додатні значення для ваги, зросту та цілей", "error");
+      return;
+    }
+
+    const updatedProfile = {
+      weight: weightVal,
+      height: heightVal,
+      targetWeight: targetWeightVal,
+      goal: tempProfile.goal,
+      targetCalories: Math.round(targetCalsVal),
+      targetProtein: Math.round(targetProteinVal),
+      targetFat: Math.round(targetFatVal),
+      targetCarbs: Math.round(targetCarbsVal)
+    };
+
+    setProfile(updatedProfile);
+
+    // Record weight to history
+    const todayStr = getTodayString();
+    setWeightHistory(prev => {
+      const exists = prev.some(item => item.date === todayStr);
+      if (exists) {
+        return prev.map(item => item.date === todayStr ? { ...item, weight: weightVal } : item);
+      } else {
+        return [...prev, { date: todayStr, weight: weightVal }];
+      }
+    });
+
+    showToast("Параметри профілю успішно збережено!", "success");
+  };
+
   // Експорт та імпорт даних користувача (Бекапи)
   const exportUserData = () => {
     try {
@@ -1478,7 +1947,7 @@ export default function App() {
   };
 
   // Розрахунок прогресу для SVG
-  const calPercent = Math.min((totals.calories / profile.targetCalories) * 100, 100);
+  const calPercent = Math.min((totals.calories / (profile.targetCalories + totalBurnedCalories)) * 100, 100);
   const radius = 58;
   const circumference = 2 * Math.PI * radius;
   const strokeDashoffset = circumference - (calPercent / 100) * circumference;
@@ -1493,7 +1962,53 @@ export default function App() {
   // Розрахунок масштабованих КБЖВ для картки результатів сканування
   const baselineWeight = scanResult ? (Number(scanResult.weight) || 200) : 200;
 
-
+  // Розрахунок даних для графіку ваги (останні 7 записів)
+  const getWeightChartData = () => {
+    const sorted = [...weightHistory]
+      .filter(w => w && w.date && typeof w.weight === 'number')
+      .sort((a, b) => a.date.localeCompare(b.date))
+      .slice(-7);
+      
+    if (sorted.length === 0) return null;
+    
+    // Якщо лише один запис, додаємо початкову точку для лінії
+    if (sorted.length === 1) {
+      const single = sorted[0];
+      const prevDate = new Date(parseLocalDate(single.date).getTime() - 24*3600*1000);
+      sorted.unshift({ date: getTodayString(prevDate), weight: single.weight });
+    }
+    
+    const weights = sorted.map(d => d.weight);
+    const minWeight = Math.min(...weights) - 0.5;
+    const maxWeight = Math.max(...weights) + 0.5;
+    const range = maxWeight - minWeight || 1;
+    
+    const width = 300;
+    const height = 100;
+    const paddingX = 25;
+    const paddingY = 15;
+    
+    const points = sorted.map((d, index) => {
+      const x = paddingX + (index / (sorted.length - 1)) * (width - 2 * paddingX);
+      const y = height - paddingY - ((d.weight - minWeight) / range) * (height - 2 * paddingY);
+      return { x, y, weight: d.weight, label: formatDateLabel(d.date) };
+    });
+    
+    let linePath = `M ${points[0].x} ${points[0].y}`;
+    for (let i = 0; i < points.length - 1; i++) {
+      const p0 = points[i];
+      const p1 = points[i + 1];
+      const cpX1 = p0.x + (p1.x - p0.x) / 2;
+      const cpY1 = p0.y;
+      const cpX2 = p0.x + (p1.x - p0.x) / 2;
+      const cpY2 = p1.y;
+      linePath += ` C ${cpX1} ${cpY1}, ${cpX2} ${cpY2}, ${p1.x} ${p1.y}`;
+    }
+    
+    const areaPath = `${linePath} L ${points[points.length - 1].x} ${height - paddingY} L ${points[0].x} ${height - paddingY} Z`;
+    
+    return { points, linePath, areaPath, width, height, minWeight, maxWeight };
+  };
 
   return (
     <div className="app-container">
@@ -1599,10 +2114,21 @@ export default function App() {
                       strokeDashoffset={strokeDashoffset}
                     />
                   </svg>
-                  <div className="circular-progress-text">
-                    <span className="calories-val">{totals.calories}</span>
-                    <span className="calories-label">ккал</span>
-                    <span className="calories-target">з {profile.targetCalories}</span>
+                  <div className="circular-progress-text" style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', top: '50%', transform: 'translateY(-50%)' }}>
+                    <span className="calories-val" style={{ fontSize: remainingCalories > 9999 ? '22px' : '28px', fontWeight: '800', lineHeight: 1 }}>{remainingCalories}</span>
+                    <span className="calories-label" style={{ fontSize: '10px', textTransform: 'uppercase', opacity: 0.6, marginTop: '2px' }}>ккал</span>
+                    <span className="calories-target" style={{ fontSize: '9px', opacity: 0.6 }}>залишилось</span>
+                  </div>
+                  <div className="calories-detail-breakdown" style={{ display: 'flex', gap: '8px', fontSize: '10px', opacity: 0.8, justifyContent: 'center', width: '100%', position: 'absolute', bottom: '-22px' }}>
+                    <span>Ціль: <strong>{profile.targetCalories}</strong></span>
+                    <span>•</span>
+                    <span>Їжа: <strong>{totals.calories}</strong></span>
+                    {totalBurnedCalories > 0 && (
+                      <>
+                        <span>•</span>
+                        <span style={{ color: '#10b981' }}>Активні: <strong>{totalBurnedCalories}</strong></span>
+                      </>
+                    )}
                   </div>
                 </div>
 
@@ -1670,28 +2196,52 @@ export default function App() {
                     return (
                       <svg 
                         viewBox="0 0 24 24" 
-                        width="28" 
-                        height="28" 
+                        width="30" 
+                        height="30" 
                         style={{ overflow: 'visible' }}
                         className="water-droplet-svg"
                       >
                         <defs>
-                          <linearGradient id="waterDropletGrad" x1="0" y1="1" x2="0" y2="0">
-                            {/* Filled part (blue gradient) */}
-                            <stop offset="0%" stopColor="#1d4ed8" />
-                            <stop offset={`${waterPercent}%`} stopColor="#3b82f6" />
-                            {/* Empty/translucent part */}
-                            <stop offset={`${waterPercent}%`} stopColor="rgba(59, 130, 246, 0.05)" />
-                            <stop offset="100%" stopColor="rgba(255, 255, 255, 0.02)" />
-                          </linearGradient>
+                          <clipPath id="waterDropletClip">
+                            <path d="M12 22C17.5 22 21 18.5 21 14C21 9 12 2.5 12 2.5C12 2.5 3 9 3 14C3 18.5 6.5 22 12 22Z" />
+                          </clipPath>
                           <filter id="waterGlow" x="-20%" y="-20%" width="140%" height="140%">
                             <feGaussianBlur stdDeviation="1.5" result="blur" />
                             <feComposite in="SourceGraphic" in2="blur" operator="over" />
                           </filter>
                         </defs>
+                        
+                        {/* Droplet Background Empty Fill & Waves (Clipped) */}
+                        <g clipPath="url(#waterDropletClip)">
+                          {/* Translucent background */}
+                          <rect x="0" y="0" width="24" height="24" fill="rgba(59, 130, 246, 0.08)" />
+                          
+                          {/* Waves translated vertically */}
+                          <g 
+                            style={{ 
+                              transform: `translateY(${22 - 19.5 * (waterPercent / 100)}px)`, 
+                              transition: 'transform 0.6s cubic-bezier(0.4, 0, 0.2, 1)' 
+                            }}
+                          >
+                            {/* Back Wave */}
+                            <path 
+                              className="water-wave-back" 
+                              d="M -24 0 Q -12 -2, 0 0 T 24 0 T 48 0 T 72 0 L 72 30 L -24 30 Z" 
+                              fill="rgba(96, 165, 250, 0.45)" 
+                            />
+                            {/* Front Wave */}
+                            <path 
+                              className="water-wave-front" 
+                              d="M -24 0 Q -12 2, 0 0 T 24 0 T 48 0 T 72 0 L 72 30 L -24 30 Z" 
+                              fill="#3b82f6" 
+                            />
+                          </g>
+                        </g>
+
+                        {/* Droplet border contour overlay */}
                         <path 
                           d="M12 22C17.5 22 21 18.5 21 14C21 9 12 2.5 12 2.5C12 2.5 3 9 3 14C3 18.5 6.5 22 12 22Z" 
-                          fill="url(#waterDropletGrad)" 
+                          fill="none" 
                           stroke={waterPercent > 0 ? '#60a5fa' : 'rgba(59, 130, 246, 0.3)'}
                           strokeWidth="1.8" 
                           strokeLinecap="round" 
@@ -1701,6 +2251,7 @@ export default function App() {
                             filter: waterPercent >= 100 ? 'url(#waterGlow)' : 'none'
                           }}
                         />
+
                         {/* 3D Reflection Highlight */}
                         <path 
                           d="M8.5 13.5C8.1 14.5 8.3 15.8 9 16.5" 
@@ -1745,6 +2296,116 @@ export default function App() {
               </div>
             </div>
 
+            {/* Weight Tracker Card */}
+            <div className="glass-card weight-tracker-card" style={{ marginTop: '16px' }}>
+              <div className="weight-header" style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '12px' }}>
+                <div>
+                  <h3 className="weight-title" style={{ fontSize: '15px', fontWeight: 600, display: 'flex', alignItems: 'center', gap: '6px', margin: 0 }}>
+                    ⚖️ Вага та прогрес
+                  </h3>
+                  {(() => {
+                    const currentWeight = weightHistory[weightHistory.length - 1]?.weight || profile.weight;
+                    const targetWeight = profile.targetWeight || 65;
+                    const diff = (currentWeight - targetWeight).toFixed(1);
+                    return (
+                      <p className="weight-progress" style={{ fontSize: '12px', opacity: 0.7, marginTop: '2px', margin: 0 }}>
+                        Поточна: <strong>{currentWeight} кг</strong> • Ціль: <strong>{targetWeight} кг</strong> (
+                        {diff > 0 ? `ще ${diff} кг до цілі` : diff < 0 ? `набрати ${Math.abs(diff)} кг` : 'ціль досягнута!'}
+                        )
+                      </p>
+                    );
+                  })()}
+                </div>
+                <button 
+                  className="btn-water-add" 
+                  onClick={() => {
+                    const currentWeight = weightHistory[weightHistory.length - 1]?.weight || profile.weight;
+                    setWeightInputVal(currentWeight.toString());
+                    setIsWeightModalOpen(true);
+                  }}
+                  title="Записати вагу"
+                  style={{ width: 'auto', padding: '6px 12px', borderRadius: '10px', fontSize: '12px', background: 'rgba(255, 255, 255, 0.08)', border: '1px solid rgba(255,255,255,0.1)', height: 'auto' }}
+                >
+                  Записати
+                </button>
+              </div>
+
+              {/* SVG Line Chart */}
+              {(() => {
+                const chartData = getWeightChartData();
+                if (!chartData) {
+                  return (
+                    <div style={{ textAlign: 'center', padding: '20px', fontSize: '12px', opacity: 0.5 }}>
+                      Додайте записи ваги, щоб побачити графік
+                    </div>
+                  );
+                }
+                const { points, linePath, areaPath, width, height } = chartData;
+                return (
+                  <div className="weight-chart-container" style={{ position: 'relative', overflow: 'visible', margin: '10px 0 5px' }}>
+                    <svg viewBox={`0 0 ${width} ${height}`} width="100%" height={height} style={{ overflow: 'visible' }}>
+                      <defs>
+                        <linearGradient id="weightChartGrad" x1="0" y1="0" x2="0" y2="1">
+                          <stop offset="0%" stopColor="rgba(16, 185, 129, 0.25)" />
+                          <stop offset="100%" stopColor="rgba(16, 185, 129, 0.0)" />
+                        </linearGradient>
+                      </defs>
+                      
+                      {/* Grid Lines */}
+                      <line x1="25" y1="15" x2="275" y2="15" stroke="rgba(255,255,255,0.05)" strokeDasharray="3" />
+                      <line x1="25" y1="50" x2="275" y2="50" stroke="rgba(255,255,255,0.05)" strokeDasharray="3" />
+                      <line x1="25" y1="85" x2="275" y2="85" stroke="rgba(255,255,255,0.05)" strokeDasharray="3" />
+
+                      {/* Area under line */}
+                      <path d={areaPath} fill="url(#weightChartGrad)" />
+
+                      {/* Bezier Line */}
+                      <path 
+                        d={linePath} 
+                        fill="none" 
+                        stroke="#10b981" 
+                        strokeWidth="2.5" 
+                        strokeLinecap="round" 
+                      />
+
+                      {/* Dots and Labels */}
+                      {points.map((p, idx) => (
+                        <g key={idx}>
+                          {/* Outer glow ring */}
+                          <circle cx={p.x} cy={p.y} r="5" fill="rgba(16, 185, 129, 0.2)" />
+                          {/* Inner circle */}
+                          <circle cx={p.x} cy={p.y} r="3" fill="#10b981" />
+                          
+                          {/* Weight label above dot */}
+                          <text 
+                            x={p.x} 
+                            y={p.y - 8} 
+                            textAnchor="middle" 
+                            fontSize="8.5" 
+                            fontWeight="700" 
+                            fill="rgba(255,255,255,0.85)"
+                          >
+                            {p.weight}
+                          </text>
+
+                          {/* Date label below chart */}
+                          <text 
+                            x={p.x} 
+                            y={height - 2} 
+                            textAnchor="middle" 
+                            fontSize="7.5" 
+                            fill="rgba(255,255,255,0.4)"
+                          >
+                            {p.label}
+                          </text>
+                        </g>
+                      ))}
+                    </svg>
+                  </div>
+                );
+              })()}
+            </div>
+
             {/* Favorites Scroll Tray */}
             {favorites.length > 0 && (
               <div className="favorites-container" style={{ marginTop: '24px' }}>
@@ -1838,7 +2499,7 @@ export default function App() {
                   { name: 'Другий перекус', icon: '🍌' },
                   { name: 'Вечеря', icon: '🥗' }
                 ];
-                return categories.map(cat => {
+                const renderedCats = categories.map(cat => {
                   const catMeals = currentDayMeals.filter(m => {
                     const normalizedCat = m.category === 'Перекус' ? 'Перший перекус' : m.category;
                     return normalizedCat === cat.name;
@@ -1965,6 +2626,74 @@ export default function App() {
                     </div>
                   );
                 });
+
+                const sportCard = (
+                  <div key="SportActivities" className="meal-category-card sport-category-card" style={{ borderLeft: '3px solid #10b981', marginTop: '16px' }}>
+                    <div className="category-header">
+                      <div className="category-title">
+                        <span>🏃</span>
+                        <span style={{ fontWeight: 700 }}>Активності та Спорт</span>
+                      </div>
+                      <div style={{ display: 'flex', alignItems: 'center', gap: '10px' }}>
+                        {totalBurnedCalories > 0 && <span className="category-total-cals" style={{ color: '#10b981', fontWeight: 600 }}>-{totalBurnedCalories} ккал</span>}
+                        <button 
+                          className="category-add-btn" 
+                          onClick={() => {
+                            setIsExerciseModalOpen(true);
+                          }}
+                          title="Додати активність"
+                          style={{ background: 'rgba(16, 185, 129, 0.15)', color: '#10b981', border: 'none', borderRadius: '50%', width: '28px', height: '28px', display: 'flex', alignItems: 'center', justifyContent: 'center', cursor: 'pointer' }}
+                        >
+                          <Plus size={16} />
+                        </button>
+                      </div>
+                    </div>
+                    
+                    {currentDayExercises.length === 0 ? (
+                      <div className="category-empty-placeholder" style={{ padding: '16px', textAlign: 'center', fontSize: '13px', color: '#94a3b8' }}>
+                        <span>Немає занять за сьогодні. </span>
+                        <span 
+                          className="category-quick-add-link"
+                          onClick={() => {
+                            setIsExerciseModalOpen(true);
+                          }}
+                          style={{ color: '#10b981', cursor: 'pointer', textDecoration: 'underline', marginLeft: '4px' }}
+                        >
+                          Додати активність
+                        </span>
+                      </div>
+                    ) : (
+                      <div className="category-meals-list" style={{ display: 'flex', flexDirection: 'column', gap: '10px', marginTop: '10px' }}>
+                        {currentDayExercises.map(ex => (
+                          <div key={ex.id} className="timeline-item" style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '10px 12px', background: 'rgba(255, 255, 255, 0.03)', borderRadius: '12px', borderLeft: '3px solid #10b981' }}>
+                            <div className="meal-info" style={{ display: 'flex', alignItems: 'center', gap: '10px' }}>
+                              <span style={{ fontSize: '20px' }}>⚡</span>
+                              <div className="meal-text" style={{ display: 'flex', flexDirection: 'column', textAlign: 'left' }}>
+                                <span className="meal-name" style={{ fontSize: '14px', fontWeight: 600 }}>{ex.name}</span>
+                                <span className="meal-meta" style={{ fontSize: '11px', color: '#94a3b8' }}>
+                                  {ex.duration} хв • спалено {ex.burnedCalories} ккал
+                                </span>
+                              </div>
+                            </div>
+                            <button 
+                              className="meal-delete-btn" 
+                              onClick={() => {
+                                setExerciseLogs(prev => prev.filter(item => item.id !== ex.id));
+                                showToast(`Активність "${ex.name}" видалено`, 'info');
+                              }} 
+                              title="Видалити"
+                              style={{ background: 'transparent', border: 'none', color: '#ef4444', cursor: 'pointer', padding: '6px' }}
+                            >
+                              <Trash2 size={16} />
+                            </button>
+                          </div>
+                        ))}
+                      </div>
+                    )}
+                  </div>
+                );
+
+                return [...renderedCats, sportCard];
               })()}
             </div>
           </div>
@@ -2554,42 +3283,114 @@ export default function App() {
                       </div>
                     </div>
                   )}
-                </>
-              )}
-              </div>
-            )}
-
-            {scannerMode === 'search' && (
-              <div className="search-db-container">
-                {/* Search Input Box */}
-                <div className="search-input-wrapper" style={{ position: 'relative' }}>
-                  <input
-                    type="text"
-                    className="search-input-field"
-                    placeholder="Пошук страв та продуктів (напр. Хліб, Борщ...)"
-                    value={searchQuery}
-                    onChange={(e) => {
-                      setSearchQuery(e.target.value);
-                      setShowSuggestions(true);
-                    }}
-                    onFocus={() => setShowSuggestions(true)}
-                    onBlur={() => setTimeout(() => setShowSuggestions(false), 200)}
-                    onKeyDown={(e) => {
-                      if (e.key === 'Enter') {
-                        e.target.blur();
-                        setShowSuggestions(false);
-                      }
-                    }}
-                    autoFocus
-                  />
 
-                  {/* Autocomplete Suggestions Dropdown */}
-                  {(() => {
-                    if (searchQuery.length === 0 || !showSuggestions) return null;
-                    
-                    const suggestions = mockFoods.filter(food => 
-                      food.name.toLowerCase().includes(searchQuery.toLowerCase()) ||
-                      (food.brand && food.brand.toLowerCase().includes(searchQuery.toLowerCase()))
+                  {barcodeNotFound && (
+                    <div className="scan-result-card" style={{ zIndex: 100 }}>
+                      <button 
+                        onClick={() => {
+                          setBarcodeNotFound(null);
+                          setBarcodeInput('');
+                        }}
+                        style={{
+                          position: 'absolute',
+                          top: '16px',
+                          right: '16px',
+                          background: 'rgba(255, 255, 255, 0.08)',
+                          border: 'none',
+                          color: '#fff',
+                          width: '32px',
+                          height: '32px',
+                          borderRadius: '50%',
+                          display: 'flex',
+                          alignItems: 'center',
+                          justifyContent: 'center',
+                          cursor: 'pointer',
+                          zIndex: 100
+                        }}
+                        title="Закрити"
+                      >
+                        <X size={16} />
+                      </button>
+                      <div className="results-header" style={{ marginBottom: '12px', display: 'block', textAlign: 'left' }}>
+                        <span className="match-badge" style={{ background: 'rgba(239, 68, 68, 0.15)', color: '#f87171' }}>Не знайдено</span>
+                        <h2 className="dish-title" style={{ fontSize: '18px', marginTop: '6px' }}>Продукт не знайдено</h2>
+                        <p style={{ fontSize: '12px', color: '#94a3b8', marginTop: '4px', lineHeight: '1.4' }}>
+                          Штрих-код <strong>{barcodeNotFound.barcode}</strong> відсутній у базі. Ви можете створити його локально, щоб NutriSnap автоматично розпізнавав його.
+                        </p>
+                      </div>
+
+                      <div style={{ marginTop: '20px', display: 'flex', flexDirection: 'column', gap: '8px' }}>
+                        <button 
+                          className="btn-primary" 
+                          style={{ background: 'linear-gradient(135deg, #10b981 0%, #059669 100%)', border: 'none' }}
+                          onClick={() => {
+                            setCustomBarcodeVal(barcodeNotFound.barcode);
+                            setCustomBarcodeName('');
+                            setCustomBarcodeCalories('0');
+                            setCustomBarcodeProtein('0');
+                            setCustomBarcodeFat('0');
+                            setCustomBarcodeCarbs('0');
+                            setCustomBarcodeWeight('100');
+                            setIsCustomBarcodeModalOpen(true);
+                            setBarcodeNotFound(null);
+                          }}
+                        >
+                          <Plus size={18} />
+                          Додати продукт вручну
+                        </button>
+                        <button 
+                          className="btn-secondary" 
+                          onClick={() => {
+                            setBarcodeNotFound(null);
+                            setBarcodeInput('');
+                            setBarcodeError(null);
+                          }}
+                        >
+                          Сканувати знову
+                        </button>
+                      </div>
+                    </div>
+                  )}
+                </>
+              )}
+              </div>
+            )}
+
+            {scannerMode === 'search' && (
+              <div className="search-db-container">
+                {/* Search Input Box */}
+                <div className="search-input-wrapper" style={{ position: 'relative' }}>
+                  <input
+                    type="text"
+                    className="search-input-field"
+                    placeholder="Пошук страв та продуктів (напр. Хліб, Борщ...)"
+                    value={searchQuery}
+                    onChange={(e) => {
+                      setSearchQuery(e.target.value);
+                      setShowSuggestions(true);
+                    }}
+                    onFocus={() => setShowSuggestions(true)}
+                    onBlur={() => setTimeout(() => setShowSuggestions(false), 200)}
+                    onKeyDown={(e) => {
+                      if (e.key === 'Enter') {
+                        e.target.blur();
+                        setShowSuggestions(false);
+                      }
+                    }}
+                    autoFocus
+                  />
+
+                  {/* Autocomplete Suggestions Dropdown */}
+                  {(() => {
+                    if (searchQuery.length === 0 || !showSuggestions) return null;
+                    
+                    const combinedSuggestions = [
+                      ...customRecipes.map(r => ({ ...r, isCustomRecipe: true, icon: '🥘', id: r.id || `recipe-${r.name}` })),
+                      ...mockFoods
+                    ];
+                    const suggestions = combinedSuggestions.filter(food => 
+                      food.name.toLowerCase().includes(searchQuery.toLowerCase()) ||
+                      (food.brand && food.brand.toLowerCase().includes(searchQuery.toLowerCase()))
                     ).slice(0, 6);
 
                     if (suggestions.length === 0) return null;
@@ -2638,7 +3439,7 @@ export default function App() {
                                 {food.name}
                               </span>
                               <span style={{ fontSize: '11px', color: '#94a3b8' }}>
-                                {food.brand ? `${food.brand} • ` : ''}{food.calories} ккал
+                                {food.isCustomRecipe ? 'Власний рецепт • ' : (food.brand ? `${food.brand} • ` : '')}{food.calories} ккал
                               </span>
                             </div>
                           </div>
@@ -2685,6 +3486,69 @@ export default function App() {
                   </div>
                 )}
 
+                {/* Recipe & Custom Food Creator Trigger Buttons */}
+                <div style={{ padding: '0 16px 10px', display: 'flex', gap: '8px' }}>
+                  <button
+                    className="btn-secondary"
+                    onClick={() => {
+                      setRecipeName('');
+                      setRecipeIngredients([]);
+                      setRecipeCookedWeight('');
+                      setRecipeSearchQuery('');
+                      setRecipeSelectedFood(null);
+                      setIsRecipeCreatorOpen(true);
+                    }}
+                    style={{
+                      flex: 1,
+                      padding: '10px 8px',
+                      borderRadius: '14px',
+                      fontSize: '12px',
+                      fontWeight: 600,
+                      background: 'rgba(16, 185, 129, 0.12)',
+                      color: '#10b981',
+                      border: '1px solid rgba(16, 185, 129, 0.2)',
+                      display: 'flex',
+                      alignItems: 'center',
+                      justifyContent: 'center',
+                      gap: '4px',
+                      cursor: 'pointer',
+                      transition: 'all 0.2s ease'
+                    }}
+                  >
+                    <span>🍳 Створити рецепт</span>
+                  </button>
+                  <button
+                    className="btn-secondary"
+                    onClick={() => {
+                      setCustomFoodName('');
+                      setCustomFoodCalories('0');
+                      setCustomFoodProtein('0');
+                      setCustomFoodFat('0');
+                      setCustomFoodCarbs('0');
+                      setCustomFoodWeight('100');
+                      setIsCustomFoodModalOpen(true);
+                    }}
+                    style={{
+                      flex: 1,
+                      padding: '10px 8px',
+                      borderRadius: '14px',
+                      fontSize: '12px',
+                      fontWeight: 600,
+                      background: 'rgba(99, 102, 241, 0.12)',
+                      color: '#818cf8',
+                      border: '1px solid rgba(99, 102, 241, 0.2)',
+                      display: 'flex',
+                      alignItems: 'center',
+                      justifyContent: 'center',
+                      gap: '4px',
+                      cursor: 'pointer',
+                      transition: 'all 0.2s ease'
+                    }}
+                  >
+                    <span>➕ Додати продукт вручну</span>
+                  </button>
+                </div>
+
                 {/* Filter Chips */}
                 <div className="filter-chips-container">
                   {['Усі', 'Супермаркети', 'Страви', 'Обрані', 'Сніданок', 'Обід', 'Вечеря', 'Перекуси'].map(filter => (
@@ -2709,7 +3573,37 @@ export default function App() {
 
                   {filteredSearchFoods.length === 0 && externalSearchFoods.length === 0 && !isSearchingExternal ? (
                     <div style={{ textAlign: 'center', color: '#94a3b8', padding: '40px 20px', fontSize: '14px' }}>
-                      Нічого не знайдено за запитом. Спробуйте змінити фільтр або ввести назву страви для оцінки ШІ вище.
+                      <p style={{ marginBottom: '14px' }}>Нічого не знайдено за запитом.</p>
+                      <button
+                        className="btn-primary"
+                        onClick={() => {
+                          setCustomFoodName(searchQuery);
+                          setCustomFoodCalories('0');
+                          setCustomFoodProtein('0');
+                          setCustomFoodFat('0');
+                          setCustomFoodCarbs('0');
+                          setCustomFoodWeight('100');
+                          setIsCustomFoodModalOpen(true);
+                        }}
+                        style={{
+                          width: 'auto',
+                          margin: '0 auto',
+                          padding: '8px 16px',
+                          fontSize: '13px',
+                          borderRadius: '10px',
+                          background: 'linear-gradient(135deg, #10b981 0%, #059669 100%)',
+                          border: 'none',
+                          display: 'inline-flex',
+                          alignItems: 'center',
+                          gap: '6px',
+                          color: 'white',
+                          fontWeight: 600,
+                          cursor: 'pointer'
+                        }}
+                      >
+                        <Plus size={16} />
+                        Створити продукт «{searchQuery}» вручну
+                      </button>
                     </div>
                   ) : (
                     <>
@@ -2729,10 +3623,12 @@ export default function App() {
                               {food.name}
                             </span>
                             <span style={{ fontSize: '11px', color: '#94a3b8' }}>
-                              {food.brand ? `${food.brand} • ` : ''}{food.calories} ккал / {food.weight}г
+                              {food.isCustomRecipe ? 'Власний рецепт • ' : (food.isCustomFood ? 'Власний продукт • ' : (food.brand ? `${food.brand} • ` : ''))}{food.calories} ккал / {food.weight}г
                             </span>
                           </div>
-                          {food.brand && <span className="search-brand-badge">{food.brand}</span>}
+                          {food.isCustomRecipe && <span className="search-brand-badge" style={{ background: 'rgba(16, 185, 129, 0.15)', color: '#10b981' }}>Рецепт</span>}
+                          {food.isCustomFood && <span className="search-brand-badge" style={{ background: 'rgba(99, 102, 241, 0.15)', color: '#818cf8' }}>Мій продукт</span>}
+                          {food.brand && !food.isCustomRecipe && !food.isCustomFood && <span className="search-brand-badge">{food.brand}</span>}
                         </div>
                       ))}
 
@@ -3055,7 +3951,16 @@ export default function App() {
         {/* ========================================================================= */}
         {activeTab === 'diary' && (
           <div>
-            <h2 className="section-title">Історія та Щоденник</h2>
+            <div style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '14px' }}>
+              <button 
+                onClick={() => changeTab('dashboard')} 
+                className="btn-back-header" 
+                title="Назад на Головну"
+              >
+                <ChevronLeft size={20} />
+              </button>
+              <h2 className="section-title" style={{ margin: 0 }}>Історія та Щоденник</h2>
+            </div>
 
             {/* Interactive Month Grid Calendar */}
             <div className="glass-card calendar-card" style={{ padding: '16px', marginBottom: '16px' }}>
@@ -3185,7 +4090,7 @@ export default function App() {
                   { name: 'Другий перекус', icon: '🍌' },
                   { name: 'Вечеря', icon: '🥗' }
                 ];
-                return categories.map(cat => {
+                const renderedCats = categories.map(cat => {
                   const catMeals = currentDayMeals.filter(m => {
                     const normalizedCat = m.category === 'Перекус' ? 'Перший перекус' : m.category;
                     return normalizedCat === cat.name;
@@ -3310,6 +4215,74 @@ export default function App() {
                     </div>
                   );
                 });
+
+                const sportCard = (
+                  <div key="SportActivities" className="meal-category-card sport-category-card" style={{ borderLeft: '3px solid #10b981', marginTop: '16px' }}>
+                    <div className="category-header">
+                      <div className="category-title">
+                        <span>🏃</span>
+                        <span style={{ fontWeight: 700 }}>Активності та Спорт</span>
+                      </div>
+                      <div style={{ display: 'flex', alignItems: 'center', gap: '10px' }}>
+                        {totalBurnedCalories > 0 && <span className="category-total-cals" style={{ color: '#10b981', fontWeight: 600 }}>-{totalBurnedCalories} ккал</span>}
+                        <button 
+                          className="category-add-btn" 
+                          onClick={() => {
+                            setIsExerciseModalOpen(true);
+                          }}
+                          title="Додати активність"
+                          style={{ background: 'rgba(16, 185, 129, 0.15)', color: '#10b981', border: 'none', borderRadius: '50%', width: '28px', height: '28px', display: 'flex', alignItems: 'center', justifyContent: 'center', cursor: 'pointer' }}
+                        >
+                          <Plus size={16} />
+                        </button>
+                      </div>
+                    </div>
+                    
+                    {currentDayExercises.length === 0 ? (
+                      <div className="category-empty-placeholder" style={{ padding: '16px', textAlign: 'center', fontSize: '13px', color: '#94a3b8' }}>
+                        <span>Немає занять за сьогодні. </span>
+                        <span 
+                          className="category-quick-add-link"
+                          onClick={() => {
+                            setIsExerciseModalOpen(true);
+                          }}
+                          style={{ color: '#10b981', cursor: 'pointer', textDecoration: 'underline', marginLeft: '4px' }}
+                        >
+                          Додати активність
+                        </span>
+                      </div>
+                    ) : (
+                      <div className="category-meals-list" style={{ display: 'flex', flexDirection: 'column', gap: '10px', marginTop: '10px' }}>
+                        {currentDayExercises.map(ex => (
+                          <div key={ex.id} className="timeline-item" style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '10px 12px', background: 'rgba(255, 255, 255, 0.03)', borderRadius: '12px', borderLeft: '3px solid #10b981' }}>
+                            <div className="meal-info" style={{ display: 'flex', alignItems: 'center', gap: '10px' }}>
+                              <span style={{ fontSize: '20px' }}>⚡</span>
+                              <div className="meal-text" style={{ display: 'flex', flexDirection: 'column', textAlign: 'left' }}>
+                                <span className="meal-name" style={{ fontSize: '14px', fontWeight: 600 }}>{ex.name}</span>
+                                <span className="meal-meta" style={{ fontSize: '11px', color: '#94a3b8' }}>
+                                  {ex.duration} хв • спалено {ex.burnedCalories} ккал
+                                </span>
+                              </div>
+                            </div>
+                            <button 
+                              className="meal-delete-btn" 
+                              onClick={() => {
+                                setExerciseLogs(prev => prev.filter(item => item.id !== ex.id));
+                                showToast(`Активність "${ex.name}" видалено`, 'info');
+                              }} 
+                              title="Видалити"
+                              style={{ background: 'transparent', border: 'none', color: '#ef4444', cursor: 'pointer', padding: '6px' }}
+                            >
+                              <Trash2 size={16} />
+                            </button>
+                          </div>
+                        ))}
+                      </div>
+                    )}
+                  </div>
+                );
+
+                return [...renderedCats, sportCard];
               })()}
             </div>
           </div>
@@ -3320,7 +4293,16 @@ export default function App() {
         {/* ========================================================================= */}
         {activeTab === 'profile' && (
           <div>
-            <h2 className="section-title">Профіль користувача</h2>
+            <div style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '14px' }}>
+              <button 
+                onClick={() => changeTab('dashboard')} 
+                className="btn-back-header" 
+                title="Назад на Головну"
+              >
+                <ChevronLeft size={20} />
+              </button>
+              <h2 className="section-title" style={{ margin: 0 }}>Профіль користувача</h2>
+            </div>
             
             {/* Profile Calculations Card */}
             <div className="glass-card">
@@ -3330,23 +4312,32 @@ export default function App() {
               </h3>
 
               <div className="settings-group">
-                <div className="profile-grid-2col">
+                <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '12px', marginBottom: '14px' }}>
+                  <div className="settings-row">
+                    <span className="settings-label">Вага (кг):</span>
+                    <input 
+                      type="number"
+                      className="settings-input"
+                      value={tempProfile.weight}
+                      onChange={(e) => handleTempProfileChange('weight', e.target.value)}
+                    />
+                  </div>
                   <div className="settings-row">
-                    <span className="settings-label">Ваша вага (кг):</span>
+                    <span className="settings-label">Ціль (кг):</span>
                     <input 
                       type="number"
                       className="settings-input"
-                      value={profile.weight}
-                      onChange={(e) => handleProfileChange('weight', e.target.value)}
+                      value={tempProfile.targetWeight}
+                      onChange={(e) => handleTempProfileChange('targetWeight', e.target.value)}
                     />
                   </div>
                   <div className="settings-row">
-                    <span className="settings-label">Ваш зріст (см):</span>
+                    <span className="settings-label">Зріст (см):</span>
                     <input 
                       type="number"
                       className="settings-input"
-                      value={profile.height}
-                      onChange={(e) => handleProfileChange('height', e.target.value)}
+                      value={tempProfile.height}
+                      onChange={(e) => handleTempProfileChange('height', e.target.value)}
                     />
                   </div>
                 </div>
@@ -3355,8 +4346,8 @@ export default function App() {
                   <span className="settings-label">Ваша фітнес-ціль:</span>
                   <select 
                     className="settings-input settings-select"
-                    value={profile.goal}
-                    onChange={(e) => handleProfileChange('goal', e.target.value)}
+                    value={tempProfile.goal}
+                    onChange={(e) => handleTempProfileChange('goal', e.target.value)}
                   >
                     <option value="lose">Схуднення (Дефіцит)</option>
                     <option value="maintain">Підтримка ваги (Баланс)</option>
@@ -3369,19 +4360,19 @@ export default function App() {
                   <div className="settings-macros-calc">
                     <div className="target-pill">
                       <span style={{ color: 'var(--color-calories)', fontSize: '10px' }}>Калорії</span>
-                      <span className="target-val">{profile.targetCalories} ккал</span>
+                      <span className="target-val">{tempProfile.targetCalories} ккал</span>
                     </div>
                     <div className="target-pill">
                       <span style={{ color: 'var(--color-protein)', fontSize: '10px' }}>Білки</span>
-                      <span className="target-val">{profile.targetProtein}г</span>
+                      <span className="target-val">{tempProfile.targetProtein}г</span>
                     </div>
                     <div className="target-pill">
                       <span style={{ color: 'var(--color-fat)', fontSize: '10px' }}>Жири</span>
-                      <span className="target-val">{profile.targetFat}г</span>
+                      <span className="target-val">{tempProfile.targetFat}г</span>
                     </div>
                     <div className="target-pill">
                       <span style={{ color: 'var(--color-carbs)', fontSize: '10px' }}>Вуглеводи</span>
-                      <span className="target-val">{profile.targetCarbs}г</span>
+                      <span className="target-val">{tempProfile.targetCarbs}г</span>
                     </div>
                   </div>
                 </div>
@@ -3397,18 +4388,8 @@ export default function App() {
                     <input 
                       type="number" 
                       className="settings-input" 
-                      value={profile.targetCalories} 
-                      onChange={(e) => {
-                        const cals = Math.max(0, Number(e.target.value) || 0);
-                        // Також пропорційно скоригуємо макроси (30/25/45)
-                        setProfile(prev => ({
-                          ...prev,
-                          targetCalories: cals,
-                          targetProtein: Math.round(cals * 0.3 / 4),
-                          targetFat: Math.round(cals * 0.25 / 9),
-                          targetCarbs: Math.round(cals * 0.45 / 4)
-                        }));
-                      }}
+                      value={tempProfile.targetCalories} 
+                      onChange={(e) => handleTempCaloriesChange(e.target.value)}
                     />
                   </div>
 
@@ -3419,20 +4400,8 @@ export default function App() {
                         type="number" 
                         className="settings-input" 
                         style={{ width: '100%', fontSize: '13px', padding: '6px', textAlign: 'center' }}
-                        value={profile.targetProtein} 
-                        onChange={(e) => {
-                          const p = Math.max(0, Number(e.target.value) || 0);
-                          setProfile(prev => {
-                            const newProt = p;
-                            const newFat = prev.targetFat;
-                            const newCarbs = Math.max(0, Math.round((prev.targetCalories - newProt * 4 - newFat * 9) / 4));
-                            return {
-                              ...prev,
-                              targetProtein: newProt,
-                              targetCarbs: newCarbs
-                            };
-                          });
-                        }}
+                        value={tempProfile.targetProtein} 
+                        onChange={(e) => handleTempProteinChange(e.target.value)}
                       />
                     </div>
                     <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: '4px' }}>
@@ -3441,20 +4410,8 @@ export default function App() {
                         type="number" 
                         className="settings-input" 
                         style={{ width: '100%', fontSize: '13px', padding: '6px', textAlign: 'center' }}
-                        value={profile.targetFat} 
-                        onChange={(e) => {
-                          const f = Math.max(0, Number(e.target.value) || 0);
-                          setProfile(prev => {
-                            const newProt = prev.targetProtein;
-                            const newFat = f;
-                            const newCarbs = Math.max(0, Math.round((prev.targetCalories - newProt * 4 - newFat * 9) / 4));
-                            return {
-                              ...prev,
-                              targetFat: newFat,
-                              targetCarbs: newCarbs
-                            };
-                          });
-                        }}
+                        value={tempProfile.targetFat} 
+                        onChange={(e) => handleTempFatChange(e.target.value)}
                       />
                     </div>
                     <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: '4px' }}>
@@ -3463,24 +4420,36 @@ export default function App() {
                         type="number" 
                         className="settings-input" 
                         style={{ width: '100%', fontSize: '13px', padding: '6px', textAlign: 'center' }}
-                        value={profile.targetCarbs} 
-                        onChange={(e) => {
-                          const c = Math.max(0, Number(e.target.value) || 0);
-                          setProfile(prev => {
-                            const newProt = prev.targetProtein;
-                            const newCarbs = c;
-                            const newFat = Math.max(0, Math.round((prev.targetCalories - newProt * 4 - newCarbs * 4) / 9));
-                            return {
-                              ...prev,
-                              targetCarbs: newCarbs,
-                              targetFat: newFat
-                            };
-                          });
-                        }}
+                        value={tempProfile.targetCarbs} 
+                        onChange={(e) => handleTempCarbsChange(e.target.value)}
                       />
                     </div>
                   </div>
                 </div>
+
+                {/* Save Changes Button */}
+                <div style={{ marginTop: '20px', borderTop: '1px solid var(--border-dark)', paddingTop: '20px' }}>
+                  <button
+                    type="button"
+                    className="btn-primary"
+                    onClick={handleSaveProfileSettings}
+                    style={{
+                      width: '100%',
+                      padding: '14px',
+                      borderRadius: '16px',
+                      background: 'linear-gradient(135deg, #10b981 0%, #059669 100%)',
+                      border: 'none',
+                      color: 'white',
+                      fontWeight: 600,
+                      cursor: 'pointer',
+                      boxShadow: '0 4px 12px rgba(16, 185, 129, 0.2)',
+                      transition: 'all 0.2s ease'
+                    }}
+                  >
+                    Зберегти параметри
+                  </button>
+                </div>
+
               </div>
             </div>
           </div>
@@ -3491,7 +4460,16 @@ export default function App() {
         {/* ========================================================================= */}
         {activeTab === 'settings' && (
           <div>
-            <h2 className="section-title">Налаштування додатку</h2>
+            <div style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '14px' }}>
+              <button 
+                onClick={() => changeTab('dashboard')} 
+                className="btn-back-header" 
+                title="Назад на Головну"
+              >
+                <ChevronLeft size={20} />
+              </button>
+              <h2 className="section-title" style={{ margin: 0 }}>Налаштування додатку</h2>
+            </div>
             
             {/* AI Scanner Configuration Card */}
             <div className="glass-card">
@@ -3562,97 +4540,107 @@ export default function App() {
                           fontSize: '12px',
                           lineHeight: '1.4'
                         }}>
-                          <strong>⚠️ Увага:</strong> Для роботи ШІ-сканера, будь ласка, отримайте свій власний безкоштовний ключ на <a href="https://aistudio.google.com/" target="_blank" rel="noreferrer" style={{ color: '#f87171', textDecoration: 'underline', fontWeight: 600 }}>Google AI Studio</a> та введіть його вище.
-                          <div style={{ marginTop: '8px' }}>
-                            <button
-                              type="button"
-                              style={{
-                                width: '100%',
-                                padding: '6px 12px',
-                                fontSize: '11px',
-                                background: 'rgba(255, 255, 255, 0.1)',
-                                border: '1px solid rgba(255, 255, 255, 0.15)',
-                                color: '#fff',
-                                borderRadius: '6px',
-                                cursor: 'pointer',
-                                fontWeight: '600',
-                                transition: 'all 0.2s'
-                              }}
-                              onClick={() => {
-                                setScanMode('mock');
-                                localStorage.setItem('nutrisnap_scanmode', 'mock');
-                                showToast("Режим сканування змінено на Симуляцію", "info");
-                              }}
-                              onMouseOver={(e) => {
-                                e.currentTarget.style.background = 'rgba(255, 255, 255, 0.15)';
-                                e.currentTarget.style.borderColor = 'rgba(255, 255, 255, 0.25)';
-                              }}
-                              onMouseOut={(e) => {
-                                e.currentTarget.style.background = 'rgba(255, 255, 255, 0.1)';
-                                e.currentTarget.style.borderColor = 'rgba(255, 255, 255, 0.15)';
-                              }}
-                            >
-                              Перемкнути на Симуляцію (Локальна база)
-                            </button>
-                          </div>
-                        </div>
-                      )}
-                    </div>
-                  </>
-                )}
+                   {/* CUSTOM BARCODE LOGGER MODAL */}
+      {isCustomBarcodeModalOpen && (
+        <div className="modal-backdrop" onClick={() => setIsCustomBarcodeModalOpen(false)}>
+          <div className="modal-content glassmorphic-modal" onClick={(e) => e.stopPropagation()}>
+            <div className="modal-header">
+              <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
+                <span style={{ fontSize: '20px' }}>🏷️</span>
+                <h3 style={{ margin: 0, fontSize: '16px', fontWeight: 700 }}>Створення продукту</h3>
               </div>
+              <button className="modal-close-btn" onClick={() => setIsCustomBarcodeModalOpen(false)} style={{ background: 'transparent', border: 'none', color: '#fff', cursor: 'pointer', padding: '4px' }}><X size={18} /></button>
             </div>
-
-            {/* Backup Configuration Card */}
-            <div className="glass-card" style={{ marginTop: '16px' }}>
-              <h3 style={{ fontSize: '15px', fontWeight: 600, marginBottom: '12px', display: 'flex', alignItems: 'center', gap: '8px' }}>
-                <Database size={18} style={{ color: 'var(--color-carbs)' }} />
-                Резервне копіювання даних
-              </h3>
+            <div className="modal-body" style={{ textAlign: 'left' }}>
+              <div className="form-group" style={{ marginBottom: '14px' }}>
+                <label className="modal-label" style={{ fontSize: '12px', display: 'block', marginBottom: '6px', opacity: 0.8 }}>Штрих-код:</label>
+                <input type="text" className="modal-input" value={customBarcodeVal} readOnly style={{ opacity: 0.6, background: 'rgba(255,255,255,0.02)', width: '100%', boxSizing: 'border-box' }} />
+              </div>
+              <div className="form-group" style={{ marginBottom: '14px' }}>
+                <label className="modal-label" style={{ fontSize: '12px', display: 'block', marginBottom: '6px', opacity: 0.8 }}>Назва продукту:</label>
+                <input type="text" className="modal-input" value={customBarcodeName} onChange={(e) => setCustomBarcodeName(e.target.value)} placeholder="Наприклад, Хліб житній" autoFocus style={{ width: '100%', boxSizing: 'border-box' }} />
+              </div>
+              <div className="form-group" style={{ marginBottom: '14px' }}>
+                <label className="modal-label" style={{ fontSize: '12px', display: 'block', marginBottom: '6px', opacity: 0.8 }}>Вага за замовчуванням (г):</label>
+                <input type="number" className="modal-input" value={customBarcodeWeight} onChange={(e) => setCustomBarcodeWeight(e.target.value)} placeholder="100" style={{ width: '100%', boxSizing: 'border-box' }} />
+              </div>
               
-              <div className="settings-group">
-                <p style={{ fontSize: '12px', color: 'var(--text-dark-muted)', marginBottom: '8px', lineHeight: '1.4' }}>
-                  Ви можете зберегти всі свої дані (профіль, історію споживання їжі та води, налаштування) у файл та згодом відновити їх на іншому пристрої.
-                </p>
-                
-                <div className="backup-btn-group">
-                  <button 
-                    className="backup-btn-export" 
-                    onClick={exportUserData}
-                  >
-                    <Download size={16} />
-                    Зберегти копію
-                  </button>
-                  
-                  <label 
-                    className="backup-btn-import"
-                  >
-                    <Upload size={16} />
-                    Відновити з файлу
-                    <input 
-                      type="file" 
-                      accept=".json" 
-                      onChange={importUserData} 
-                      style={{ display: 'none' }} 
-                    />
-                  </label>
+              <h4 style={{ fontSize: '13px', color: 'var(--text-dark-secondary)', margin: '14px 0 8px', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '4px' }}>Показники на 100г</h4>
+              <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '10px' }}>
+                <div className="form-group">
+                  <label className="modal-label" style={{ color: 'var(--color-calories)', fontSize: '11px', display: 'block', marginBottom: '4px' }}>Калорійність (ккал):</label>
+                  <input type="number" className="modal-input" value={customBarcodeCalories} onChange={(e) => setCustomBarcodeCalories(e.target.value)} placeholder="0" style={{ width: '100%', boxSizing: 'border-box' }} />
+                </div>
+                <div className="form-group">
+                  <label className="modal-label" style={{ color: 'var(--color-protein)', fontSize: '11px', display: 'block', marginBottom: '4px' }}>Білки (г):</label>
+                  <input type="number" step="0.1" className="modal-input" value={customBarcodeProtein} onChange={(e) => setCustomBarcodeProtein(e.target.value)} placeholder="0" style={{ width: '100%', boxSizing: 'border-box' }} />
+                </div>
+                <div className="form-group">
+                  <label className="modal-label" style={{ color: 'var(--color-fat)', fontSize: '11px', display: 'block', marginBottom: '4px' }}>Жири (г):</label>
+                  <input type="number" step="0.1" className="modal-input" value={customBarcodeFat} onChange={(e) => setCustomBarcodeFat(e.target.value)} placeholder="0" style={{ width: '100%', boxSizing: 'border-box' }} />
+                </div>
+                <div className="form-group">
+                  <label className="modal-label" style={{ color: 'var(--color-carbs)', fontSize: '11px', display: 'block', marginBottom: '4px' }}>Вуглеводи (г):</label>
+                  <input type="number" step="0.1" className="modal-input" value={customBarcodeCarbs} onChange={(e) => setCustomBarcodeCarbs(e.target.value)} placeholder="0" style={{ width: '100%', boxSizing: 'border-box' }} />
                 </div>
               </div>
             </div>
+            <div className="modal-footer" style={{ display: 'flex', gap: '10px', marginTop: '20px', justifyContent: 'flex-end' }}>
+              <button className="btn-secondary" onClick={() => setIsCustomBarcodeModalOpen(false)} style={{ width: 'auto', padding: '10px 18px' }}>Скасувати</button>
+              <button className="btn-primary" onClick={handleSaveCustomBarcode} style={{ width: 'auto', padding: '10px 18px' }}>Зберегти продукт</button>
+            </div>
+          </div>
+        </div>
+      )}
 
-            {/* Technical Information / Credits */}
-            <div style={{ textAlign: 'center', padding: '15px 0', fontSize: '11px', color: 'var(--text-dark-muted)', display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '8px' }}>
-              <p>NutriSnap v1.1.0 (Оновлено: Safe Area + Top Toasts)</p>
-              <p>Працює локально на вашому пристрої.</p>
-              <button
-                onClick={() => {
-                  if ('serviceWorker' in navigator) {
-                    navigator.serviceWorker.getRegistrations().then((registrations) => {
-                      for (let registration of registrations) {
-                        registration.unregister();
-                      }
-                      if ('caches' in window) {
-                        caches.keys().then((names) => {
+      {/* CUSTOM FOOD LOGGER MODAL */}
+      {isCustomFoodModalOpen && (
+        <div className="modal-backdrop" onClick={() => setIsCustomFoodModalOpen(false)}>
+          <div className="modal-content glassmorphic-modal" onClick={(e) => e.stopPropagation()}>
+            <div className="modal-header">
+              <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
+                <span style={{ fontSize: '20px' }}>🏷️</span>
+                <h3 style={{ margin: 0, fontSize: '16px', fontWeight: 700 }}>Створення продукту</h3>
+              </div>
+              <button className="modal-close-btn" onClick={() => setIsCustomFoodModalOpen(false)} style={{ background: 'transparent', border: 'none', color: '#fff', cursor: 'pointer', padding: '4px' }}><X size={18} /></button>
+            </div>
+            <div className="modal-body" style={{ textAlign: 'left' }}>
+              <div className="form-group" style={{ marginBottom: '14px' }}>
+                <label className="modal-label" style={{ fontSize: '12px', display: 'block', marginBottom: '6px', opacity: 0.8 }}>Назва продукту:</label>
+                <input type="text" className="modal-input" value={customFoodName} onChange={(e) => setCustomFoodName(e.target.value)} placeholder="Наприклад, Сирок сирковий" autoFocus style={{ width: '100%', boxSizing: 'border-box' }} />
+              </div>
+              <div className="form-group" style={{ marginBottom: '14px' }}>
+                <label className="modal-label" style={{ fontSize: '12px', display: 'block', marginBottom: '6px', opacity: 0.8 }}>Вага за замовчуванням (г):</label>
+                <input type="number" className="modal-input" value={customFoodWeight} onChange={(e) => setCustomFoodWeight(e.target.value)} placeholder="100" style={{ width: '100%', boxSizing: 'border-box' }} />
+              </div>
+              
+              <h4 style={{ fontSize: '13px', color: 'var(--text-dark-secondary)', margin: '14px 0 8px', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '4px' }}>Показники на 100г</h4>
+              <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '10px' }}>
+                <div className="form-group">
+                  <label className="modal-label" style={{ color: 'var(--color-calories)', fontSize: '11px', display: 'block', marginBottom: '4px' }}>Калорійність (ккал):</label>
+                  <input type="number" className="modal-input" value={customFoodCalories} onChange={(e) => setCustomFoodCalories(e.target.value)} placeholder="0" style={{ width: '100%', boxSizing: 'border-box' }} />
+                </div>
+                <div className="form-group">
+                  <label className="modal-label" style={{ color: 'var(--color-protein)', fontSize: '11px', display: 'block', marginBottom: '4px' }}>Білки (г):</label>
+                  <input type="number" step="0.1" className="modal-input" value={customFoodProtein} onChange={(e) => setCustomFoodProtein(e.target.value)} placeholder="0" style={{ width: '100%', boxSizing: 'border-box' }} />
+                </div>
+                <div className="form-group">
+                  <label className="modal-label" style={{ color: 'var(--color-fat)', fontSize: '11px', display: 'block', marginBottom: '4px' }}>Жири (г):</label>
+                  <input type="number" step="0.1" className="modal-input" value={customFoodFat} onChange={(e) => setCustomFoodFat(e.target.value)} placeholder="0" style={{ width: '100%', boxSizing: 'border-box' }} />
+                </div>
+                <div className="form-group">
+                  <label className="modal-label" style={{ color: 'var(--color-carbs)', fontSize: '11px', display: 'block', marginBottom: '4px' }}>Вуглеводи (г):</label>
+                  <input type="number" step="0.1" className="modal-input" value={customFoodCarbs} onChange={(e) => setCustomFoodCarbs(e.target.value)} placeholder="0" style={{ width: '100%', boxSizing: 'border-box' }} />
+                </div>
+              </div>
+            </div>
+            <div className="modal-footer" style={{ display: 'flex', gap: '10px', marginTop: '20px', justifyContent: 'flex-end' }}>
+              <button className="btn-secondary" onClick={() => setIsCustomFoodModalOpen(false)} style={{ width: 'auto', padding: '10px 18px' }}>Скасувати</button>
+              <button className="btn-primary" onClick={handleSaveCustomFood} style={{ width: 'auto', padding: '10px 18px' }}>Зберегти продукт</button>
+            </div>
+          </div>
+        </div>
+      )}                   caches.keys().then((names) => {
                           Promise.all(names.map(name => caches.delete(name))).then(() => {
                             window.location.reload(true);
                           });
@@ -3734,10 +4722,557 @@ export default function App() {
             onClick={() => setActiveTab('settings')}
           >
             <Settings size={22} />
-            <span>Налаштування</span>
+            <span>Опції</span>
           </button>
         </nav>
       )}
+
+      {/* ========================================================================= */}
+      {/* GLOBAL MODALS & OVERLAYS */}
+      {/* ========================================================================= */}
+      
+      {/* CUSTOM BARCODE LOGGER MODAL */}
+      {isCustomBarcodeModalOpen && (
+        <div className="modal-backdrop" onClick={() => setIsCustomBarcodeModalOpen(false)}>
+          <div className="modal-content glassmorphic-modal" onClick={(e) => e.stopPropagation()}>
+            <div className="modal-header">
+              <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
+                <span style={{ fontSize: '20px' }}>🏷️</span>
+                <h3 style={{ margin: 0, fontSize: '16px', fontWeight: 700 }}>Створення продукту</h3>
+              </div>
+              <button className="modal-close-btn" onClick={() => setIsCustomBarcodeModalOpen(false)} style={{ background: 'transparent', border: 'none', color: '#fff', cursor: 'pointer', padding: '4px' }}><X size={18} /></button>
+            </div>
+            <div className="modal-body" style={{ textAlign: 'left' }}>
+              <div className="form-group" style={{ marginBottom: '14px' }}>
+                <label className="modal-label" style={{ fontSize: '12px', display: 'block', marginBottom: '6px', opacity: 0.8 }}>Штрих-код:</label>
+                <input type="text" className="modal-input" value={customBarcodeVal} readOnly style={{ opacity: 0.6, background: 'rgba(255,255,255,0.02)', width: '100%', boxSizing: 'border-box' }} />
+              </div>
+              <div className="form-group" style={{ marginBottom: '14px' }}>
+                <label className="modal-label" style={{ fontSize: '12px', display: 'block', marginBottom: '6px', opacity: 0.8 }}>Назва продукту:</label>
+                <input type="text" className="modal-input" value={customBarcodeName} onChange={(e) => setCustomBarcodeName(e.target.value)} placeholder="Наприклад, Хліб житній" autoFocus style={{ width: '100%', boxSizing: 'border-box' }} />
+              </div>
+              <div className="form-group" style={{ marginBottom: '14px' }}>
+                <label className="modal-label" style={{ fontSize: '12px', display: 'block', marginBottom: '6px', opacity: 0.8 }}>Вага за замовчуванням (г):</label>
+                <input type="number" className="modal-input" value={customBarcodeWeight} onChange={(e) => setCustomBarcodeWeight(e.target.value)} placeholder="100" style={{ width: '100%', boxSizing: 'border-box' }} />
+              </div>
+              
+              <h4 style={{ fontSize: '13px', color: 'var(--text-dark-secondary)', margin: '14px 0 8px', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '4px' }}>Показники на 100г</h4>
+              <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: '10px' }}>
+                <div className="form-group">
+                  <label className="modal-label" style={{ color: 'var(--color-calories)', fontSize: '11px', display: 'block', marginBottom: '4px' }}>Калорійність (ккал):</label>
+                  <input type="number" className="modal-input" value={customBarcodeCalories} onChange={(e) => setCustomBarcodeCalories(e.target.value)} placeholder="0" style={{ width: '100%', boxSizing: 'border-box' }} />
+                </div>
+                <div className="form-group">
+                  <label className="modal-label" style={{ color: 'var(--color-protein)', fontSize: '11px', display: 'block', marginBottom: '4px' }}>Білки (г):</label>
+                  <input type="number" step="0.1" className="modal-input" value={customBarcodeProtein} onChange={(e) => setCustomBarcodeProtein(e.target.value)} placeholder="0" style={{ width: '100%', boxSizing: 'border-box' }} />
+                </div>
+                <div className="form-group">
+                  <label className="modal-label" style={{ color: 'var(--color-fat)', fontSize: '11px', display: 'block', marginBottom: '4px' }}>Жири (г):</label>
+                  <input type="number" step="0.1" className="modal-input" value={customBarcodeFat} onChange={(e) => setCustomBarcodeFat(e.target.value)} placeholder="0" style={{ width: '100%', boxSizing: 'border-box' }} />
+                </div>
+                <div className="form-group">
+                  <label className="modal-label" style={{ color: 'var(--color-carbs)', fontSize: '11px', display: 'block', marginBottom: '4px' }}>Вуглеводи (г):</label>
+                  <input type="number" step="0.1" className="modal-input" value={customBarcodeCarbs} onChange={(e) => setCustomBarcodeCarbs(e.target.value)} placeholder="0" style={{ width: '100%', boxSizing: 'border-box' }} />
+                </div>
+              </div>
+            </div>
+            <div className="modal-footer" style={{ display: 'flex', gap: '10px', marginTop: '20px', justifyContent: 'flex-end' }}>
+              <button className="btn-secondary" onClick={() => setIsCustomBarcodeModalOpen(false)} style={{ width: 'auto', padding: '10px 18px' }}>Скасувати</button>
+              <button className="btn-primary" onClick={handleSaveCustomBarcode} style={{ width: 'auto', padding: '10px 18px' }}>Зберегти продукт</button>
+            </div>
+          </div>
+        </div>
+      )}
+
+      {/* WEIGHT LOGGER MODAL */}
+      {isWeightModalOpen && (
+        <div className="modal-backdrop" onClick={() => setIsWeightModalOpen(false)}>
+          <div className="modal-content glassmorphic-modal" onClick={(e) => e.stopPropagation()}>
+            <div className="modal-header">
+              <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
+                <span style={{ fontSize: '20px' }}>📈</span>
+                <h3 style={{ margin: 0, fontSize: '16px', fontWeight: 700 }}>Записати вагу</h3>
+              </div>
+              <button className="modal-close-btn" onClick={() => setIsWeightModalOpen(false)} style={{ background: 'transparent', border: 'none', color: '#fff', cursor: 'pointer', padding: '4px' }}><X size={18} /></button>
+            </div>
+            <div className="modal-body" style={{ textAlign: 'left' }}>
+              <div className="form-group">
+                <label className="modal-label" style={{ fontSize: '12px', display: 'block', marginBottom: '6px', opacity: 0.8 }}>Ваша вага сьогодні (кг):</label>
+                <input 
+                  type="number" 
+                  step="0.1" 
+                  className="modal-input" 
+                  value={weightInputVal} 
+                  onChange={(e) => setWeightInputVal(e.target.value)} 
+                  placeholder="70.0" 
+                  autoFocus 
+                  style={{ width: '100%', boxSizing: 'border-box' }}
+                />
+              </div>
+            </div>
+            <div className="modal-footer" style={{ display: 'flex', gap: '10px', marginTop: '20px', justifyContent: 'flex-end' }}>
+              <button className="btn-secondary" onClick={() => setIsWeightModalOpen(false)} style={{ width: 'auto', padding: '10px 18px' }}>Скасувати</button>
+              <button className="btn-primary" style={{ width: 'auto', padding: '10px 18px' }} onClick={() => {
+                const val = parseFloat(weightInputVal);
+                if (!val || val <= 0) {
+                  showToast("Будь ласка, введіть коректне значення ваги", "error");
+                  return;
+                }
+                const todayStr = getTodayString();
+                setWeightHistory(prev => {
+                  const exists = prev.some(item => item.date === todayStr);
+                  if (exists) {
+                    return prev.map(item => item.date === todayStr ? { ...item, weight: val } : item);
+                  } else {
+                    return [...prev, { date: todayStr, weight: val }];
+                  }
+                });
+                setProfile(prev => {
+                  const updated = { ...prev, weight: val };
+                  let baseCals = 2000;
+                  if (updated.goal === 'lose') {
+                    baseCals = Math.round(val * 24);
+                  } else if (updated.goal === 'gain') {
+                    baseCals = Math.round(val * 35);
+                  } else {
+                    baseCals = Math.round(val * 30);
+                  }
+                  updated.targetCalories = baseCals;
+                  updated.targetProtein = Math.round(baseCals * 0.3 / 4);
+                  updated.targetFat = Math.round(baseCals * 0.25 / 9);
+                  updated.targetCarbs = Math.round(baseCals * 0.45 / 4);
+                  return updated;
+                });
+                showToast(`Вагу ${val} кг успішно записано!`, "success");
+                setIsWeightModalOpen(false);
+              }}>Зберегти</button>
+            </div>
+          </div>
+        </div>
+      )}
+
+      {/* SPORT/EXERCISE LOGGER MODAL */}
+      {isExerciseModalOpen && (
+        <div className="modal-backdrop" onClick={() => {
+          setIsExerciseModalOpen(false);
+          setSelectedExercise(null);
+          setExerciseSearchQuery('');
+        }}>
+          <div className="modal-content glassmorphic-modal" onClick={(e) => e.stopPropagation()}>
+            <div className="modal-header">
+              <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
+                <span style={{ fontSize: '20px' }}>🏃</span>
+                <h3 style={{ margin: 0, fontSize: '16px', fontWeight: 700 }}>Додати спорт/активність</h3>
+              </div>
+              <button className="modal-close-btn" onClick={() => {
+                setIsExerciseModalOpen(false);
+                setSelectedExercise(null);
+                setExerciseSearchQuery('');
+              }} style={{ background: 'transparent', border: 'none', color: '#fff', cursor: 'pointer', padding: '4px' }}><X size={18} /></button>
+            </div>
+            <div className="modal-body" style={{ textAlign: 'left' }}>
+              <div style={{ display: 'flex', background: 'rgba(255,255,255,0.04)', borderRadius: '10px', padding: '4px', marginBottom: '14px' }}>
+                <button 
+                  type="button" 
+                  onClick={() => setIsCustomExercise(false)}
+                  style={{ flex: 1, padding: '8px', border: 'none', background: !isCustomExercise ? 'rgba(255,255,255,0.08)' : 'transparent', color: '#fff', fontSize: '12px', fontWeight: 600, borderRadius: '8px', cursor: 'pointer' }}
+                >
+                  Каталог
+                </button>
+                <button 
+                  type="button" 
+                  onClick={() => setIsCustomExercise(true)}
+                  style={{ flex: 1, padding: '8px', border: 'none', background: isCustomExercise ? 'rgba(255,255,255,0.08)' : 'transparent', color: '#fff', fontSize: '12px', fontWeight: 600, borderRadius: '8px', cursor: 'pointer' }}
+                >
+                  Власна активність
+                </button>
+              </div>
+
+              {!isCustomExercise ? (
+                <>
+                  {!selectedExercise ? (
+                    <div>
+                      <input 
+                        type="text" 
+                        className="modal-input" 
+                        placeholder="Шукати активність (напр. біг, ходьба)..." 
+                        value={exerciseSearchQuery} 
+                        onChange={(e) => setExerciseSearchQuery(e.target.value)} 
+                        autoFocus 
+                        style={{ width: '100%', boxSizing: 'border-box' }}
+                      />
+                      <div className="activities-list" style={{ marginTop: '10px', maxHeight: '180px', overflowY: 'auto', display: 'flex', flexDirection: 'column', gap: '6px' }}>
+                        {ACTIVITIES.filter(act => act.name.toLowerCase().includes(exerciseSearchQuery.toLowerCase())).map(act => (
+                          <div 
+                            key={act.name} 
+                            onClick={() => setSelectedExercise(act)}
+                            style={{ display: 'flex', alignItems: 'center', gap: '10px', padding: '10px 12px', background: 'rgba(255,255,255,0.02)', borderRadius: '10px', cursor: 'pointer', border: '1px solid rgba(255,255,255,0.05)', transition: 'background 0.2s' }}
+                          >
+                            <span style={{ fontSize: '18px' }}>{act.emoji}</span>
+                            <div style={{ flex: 1, textAlign: 'left' }}>
+                              <span style={{ fontSize: '14px', fontWeight: 600 }}>{act.name}</span>
+                              <span style={{ fontSize: '11px', opacity: 0.5, display: 'block' }}>MET: {act.met}</span>
+                            </div>
+                            <span style={{ fontSize: '12px', color: '#10b981' }}>Обрати →</span>
+                          </div>
+                        ))}
+                      </div>
+                    </div>
+                  ) : (
+                    <div style={{ textAlign: 'left', animation: 'slide-up-sheet 0.2s ease-out' }}>
+                      <div style={{ display: 'flex', alignItems: 'center', gap: '10px', padding: '12px', background: 'rgba(16, 185, 129, 0.08)', borderRadius: '12px', marginBottom: '14px' }}>
+                        <span style={{ fontSize: '24px' }}>{selectedExercise.emoji}</span>
+                        <div>
+                          <strong style={{ display: 'block', fontSize: '15px' }}>{selectedExercise.name}</strong>
+                          <span style={{ fontSize: '11px', opacity: 0.6 }}>Коефіцієнт MET: {selectedExercise.met}</span>
+                        </div>
+                        <button 
+                          className="btn-secondary" 
+                          onClick={() => setSelectedExercise(null)} 
+                          style={{ marginLeft: 'auto', width: 'auto', padding: '4px 8px', fontSize: '11px', height: 'auto', margin: 0 }}
+                        >
+                          Змінити
+                        </button>
+                      </div>
+
+                      <div className="form-group" style={{ marginBottom: '14px' }}>
+                        <label className="modal-label" style={{ fontSize: '12px', display: 'block', marginBottom: '6px' }}>Тривалість заняття (хвилин):</label>
+                        <input 
+                          type="number" 
+                          className="modal-input" 
+                          value={exerciseDuration} 
+                          onChange={(e) => setExerciseDuration(e.target.value)} 
+                          placeholder="30" 
+                          min="1"
+                          autoFocus
+                          style={{ width: '100%', boxSizing: 'border-box' }}
+                        />
+                      </div>
+
+                      {(() => {
+                        const currentWeight = weightHistory[weightHistory.length - 1]?.weight || profile.weight || 70;
+                        const durationMins = parseInt(exerciseDuration) || 0;
+                        const burned = Math.round(selectedExercise.met * currentWeight * (durationMins / 60));
+                        return (
+                          <div style={{ padding: '14px', background: 'rgba(255,255,255,0.03)', borderRadius: '12px', textAlign: 'center', border: '1px solid rgba(255,255,255,0.05)' }}>
+                            <span style={{ display: 'block', fontSize: '11px', opacity: 0.6 }}>Буде спалено приблизно:</span>
+                            <strong style={{ fontSize: '22px', color: '#10b981' }}>{burned} ккал</strong>
+                            <span style={{ display: 'block', fontSize: '10px', opacity: 0.4, marginTop: '4px' }}>(Розраховано для ваги {currentWeight} кг)</span>
+                          </div>
+                        );
+                      })()}
+                    </div>
+                  )}
+                </>
+              ) : (
+                <div style={{ textAlign: 'left' }}>
+                  <div className="form-group" style={{ marginBottom: '12px' }}>
+                    <label className="modal-label" style={{ fontSize: '12px', display: 'block', marginBottom: '6px' }}>Назва активності:</label>
+                    <input 
+                      type="text" 
+                      className="modal-input" 
+                      placeholder="Напр., Танці, Прибирання" 
+                      value={customExerciseName} 
+                      onChange={(e) => setCustomExerciseName(e.target.value)} 
+                      autoFocus
+                      style={{ width: '100%', boxSizing: 'border-box' }}
+                    />
+                  </div>
+                  <div className="form-group" style={{ marginBottom: '12px' }}>
+                    <label className="modal-label" style={{ fontSize: '12px', display: 'block', marginBottom: '6px' }}>Тривалість (хвилин):</label>
+                    <input 
+                      type="number" 
+                      className="modal-input" 
+                      placeholder="30" 
+                      value={exerciseDuration} 
+                      onChange={(e) => setExerciseDuration(e.target.value)} 
+                      style={{ width: '100%', boxSizing: 'border-box' }}
+                    />
+                  </div>
+                  <div className="form-group" style={{ marginBottom: '12px' }}>
+                    <label className="modal-label" style={{ fontSize: '12px', display: 'block', marginBottom: '6px' }}>Спалено калорій (ккал):</label>
+                    <input 
+                      type="number" 
+                      className="modal-input" 
+                      placeholder="150" 
+                      value={customExerciseCalories} 
+                      onChange={(e) => setCustomExerciseCalories(e.target.value)} 
+                      style={{ width: '100%', boxSizing: 'border-box' }}
+                    />
+                  </div>
+                </div>
+              )}
+            </div>
+            <div className="modal-footer" style={{ display: 'flex', gap: '10px', marginTop: '20px', justifyContent: 'flex-end' }}>
+              <button className="btn-secondary" onClick={() => {
+                setIsExerciseModalOpen(false);
+                setSelectedExercise(null);
+                setExerciseSearchQuery('');
+              }} style={{ width: 'auto', padding: '10px 18px' }}>Скасувати</button>
+              <button 
+                className="btn-primary" 
+                style={{ width: 'auto', padding: '10px 18px' }}
+                onClick={() => {
+                  const currentWeight = weightHistory[weightHistory.length - 1]?.weight || profile.weight || 70;
+                  const mins = parseInt(exerciseDuration) || 30;
+                  
+                  if (isCustomExercise) {
+                    if (!customExerciseName.trim()) {
+                      showToast("Введіть назву активності", "error");
+                      return;
+                    }
+                    const burnedCals = Math.round(Number(customExerciseCalories)) || 0;
+                    const newLog = {
+                      id: Date.now().toString(),
+                      name: customExerciseName.trim(),
+                      duration: mins,
+                      burnedCalories: burnedCals,
+                      date: selectedDate
+                    };
+                    setExerciseLogs(prev => [...prev, newLog]);
+                    showToast(`Активність "${customExerciseName.trim()}" додано!`, 'success');
+                  } else {
+                    if (!selectedExercise) {
+                      showToast("Оберіть активність із списку", "error");
+                      return;
+                    }
+                    const burnedCals = Math.round(selectedExercise.met * currentWeight * (mins / 60));
+                    const newLog = {
+                      id: Date.now().toString(),
+                      name: selectedExercise.name,
+                      duration: mins,
+                      burnedCalories: burnedCals,
+                      date: selectedDate
+                    };
+                    setExerciseLogs(prev => [...prev, newLog]);
+                    showToast(`Активність "${selectedExercise.name}" додано!`, 'success');
+                  }
+                  
+                  setIsExerciseModalOpen(false);
+                  setSelectedExercise(null);
+                  setExerciseSearchQuery('');
+                  setCustomExerciseName('');
+                  setCustomExerciseCalories('150');
+                }}
+              >
+                Додати активність
+              </button>
+            </div>
+          </div>
+        </div>
+      )}
+
+      {/* RECIPE CREATOR MODAL */}
+      {isRecipeCreatorOpen && (
+        <div className="modal-backdrop" onClick={() => setIsRecipeCreatorOpen(false)}>
+          <div className="modal-content glassmorphic-modal" style={{ maxWidth: '500px' }} onClick={(e) => e.stopPropagation()}>
+            <div className="modal-header">
+              <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
+                <span style={{ fontSize: '20px' }}>🍳</span>
+                <h3 style={{ margin: 0, fontSize: '16px', fontWeight: 700 }}>Конструктор рецептів</h3>
+              </div>
+              <button className="modal-close-btn" onClick={() => setIsRecipeCreatorOpen(false)} style={{ background: 'transparent', border: 'none', color: '#fff', cursor: 'pointer', padding: '4px' }}><X size={18} /></button>
+            </div>
+            <div className="modal-body" style={{ maxHeight: '70vh', overflowY: 'auto' }}>
+              <div className="form-group" style={{ marginBottom: '14px', textAlign: 'left' }}>
+                <label className="modal-label" style={{ fontSize: '12px', display: 'block', marginBottom: '6px' }}>Назва страви:</label>
+                <input 
+                  type="text" 
+                  className="modal-input" 
+                  placeholder="Наприклад: Домашній курячий суп" 
+                  value={recipeName} 
+                  onChange={(e) => setRecipeName(e.target.value)} 
+                  autoFocus 
+                  style={{ width: '100%', boxSizing: 'border-box' }}
+                />
+              </div>
+
+              {/* Ingredient Search and Adding Box */}
+              <div style={{ padding: '12px', background: 'rgba(255,255,255,0.02)', border: '1px solid rgba(255,255,255,0.05)', borderRadius: '14px', marginBottom: '16px', textAlign: 'left' }}>
+                <label className="modal-label" style={{ fontWeight: 600, display: 'block', marginBottom: '6px', fontSize: '12px' }}>Додати інгредієнт:</label>
+                
+                {!recipeSelectedFood ? (
+                  <div style={{ position: 'relative' }}>
+                    <input 
+                      type="text" 
+                      className="modal-input" 
+                      placeholder="Шукати інгредієнт у базі..." 
+                      value={recipeSearchQuery} 
+                      onChange={(e) => {
+                        setRecipeSearchQuery(e.target.value);
+                        setIsRecipeSearchActive(true);
+                      }}
+                      onFocus={() => setIsRecipeSearchActive(true)}
+                      style={{ width: '100%', boxSizing: 'border-box' }}
+                    />
+                    
+                    {/* Tiny dropdown inside modal */}
+                    {recipeSearchQuery.length >= 2 && isRecipeSearchActive && (() => {
+                      const suggestions = mockFoods.filter(food => 
+                        food.name.toLowerCase().includes(recipeSearchQuery.toLowerCase())
+                      ).slice(0, 5);
+                      
+                      if (suggestions.length === 0) return null;
+                      
+                      return (
+                        <div style={{
+                          position: 'absolute',
+                          top: '100%',
+                          left: 0,
+                          right: 0,
+                          background: theme === 'light' ? '#ffffff' : '#1e293b',
+                          borderRadius: '10px',
+                          boxShadow: '0 8px 20px rgba(0,0,0,0.3)',
+                          border: theme === 'light' ? '1px solid #e2e8f0' : '1px solid #334155',
+                          zIndex: 150,
+                          maxHeight: '150px',
+                          overflowY: 'auto'
+                        }}>
+                          {suggestions.map(food => (
+                            <div 
+                              key={food.id}
+                              onClick={() => {
+                                setRecipeSelectedFood(food);
+                                setIsRecipeSearchActive(false);
+                              }}
+                              style={{ padding: '8px 12px', cursor: 'pointer', borderBottom: '1px solid rgba(255,255,255,0.05)', fontSize: '13px', textAlign: 'left', color: theme === 'light' ? '#000' : '#fff' }}
+                            >
+                              {food.name} ({food.calories} ккал / 100г)
+                            </div>
+                          ))}
+                        </div>
+                      );
+                    })()}
+                  </div>
+                ) : (
+                  <div style={{ display: 'flex', flexDirection: 'column', gap: '10px', animation: 'slide-up-sheet 0.2s ease-out' }}>
+                    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', background: 'rgba(255,255,255,0.04)', padding: '8px 12px', borderRadius: '8px' }}>
+                      <span style={{ fontSize: '13px', fontWeight: 600 }}>{recipeSelectedFood.name}</span>
+                      <button className="btn-secondary" style={{ width: 'auto', padding: '2px 6px', fontSize: '10px', height: 'auto', margin: 0 }} onClick={() => setRecipeSelectedFood(null)}>Скасувати</button>
+                    </div>
+                    <div style={{ display: 'flex', gap: '8px', alignItems: 'center' }}>
+                      <div style={{ flex: 1 }}>
+                        <label className="modal-label" style={{ fontSize: '11px', display: 'block', marginBottom: '4px' }}>Вага в сирому вигляді (г):</label>
+                        <input 
+                          type="number" 
+                          className="modal-input" 
+                          value={recipeIngredientWeight} 
+                          onChange={(e) => setRecipeIngredientWeight(e.target.value)} 
+                          placeholder="100" 
+                          style={{ width: '100%', boxSizing: 'border-box' }}
+                        />
+                      </div>
+                      <button 
+                        className="btn-primary" 
+                        style={{ marginTop: '18px', padding: '10px 14px', height: '38px', fontSize: '12px', width: 'auto' }}
+                        onClick={() => handleAddRecipeIngredient(recipeSelectedFood, recipeIngredientWeight)}
+                      >
+                        Додати
+                      </button>
+                    </div>
+                  </div>
+                )}
+              </div>
+
+              {/* Added Ingredients List */}
+              <div style={{ textAlign: 'left', marginBottom: '16px' }}>
+                <label className="modal-label" style={{ fontWeight: 600, display: 'block', marginBottom: '6px', fontSize: '12px' }}>Інгредієнти в рецепті:</label>
+                {recipeIngredients.length === 0 ? (
+                  <div style={{ textAlign: 'center', padding: '16px', border: '1px dashed rgba(255,255,255,0.08)', borderRadius: '12px', fontSize: '12px', color: '#94a3b8' }}>
+                    Немає доданих інгредієнтів. Скористайтеся пошуком вище.
+                  </div>
+                ) : (
+                  <div style={{ display: 'flex', flexDirection: 'column', gap: '6px', maxHeight: '160px', overflowY: 'auto' }}>
+                    {recipeIngredients.map((item, index) => {
+                      const calContribution = Math.round(item.food.calories * item.rawWeight / 100);
+                      return (
+                        <div key={index} style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '8px 12px', background: 'rgba(255,255,255,0.02)', border: '1px solid rgba(255,255,255,0.04)', borderRadius: '10px' }}>
+                          <div style={{ textAlign: 'left' }}>
+                            <span style={{ fontSize: '13px', fontWeight: 600 }}>{item.food.name}</span>
+                            <span style={{ display: 'block', fontSize: '11px', color: '#94a3b8' }}>{item.rawWeight}г • {calContribution} ккал</span>
+                          </div>
+                          <button 
+                            onClick={() => handleRemoveRecipeIngredient(index)}
+                            style={{ background: 'transparent', border: 'none', color: '#ef4444', cursor: 'pointer', padding: '4px' }}
+                            title="Видалити"
+                          >
+                            <Trash2 size={14} />
+                          </button>
+                        </div>
+                      );
+                    })}
+                  </div>
+                )}
+              </div>
+
+              {/* Recipe Summary & Evaporation Calculator */}
+              {recipeIngredients.length > 0 && (() => {
+                const totalRawWeight = recipeIngredients.reduce((sum, item) => sum + item.rawWeight, 0);
+                const totalRawCalories = recipeIngredients.reduce((sum, item) => sum + (item.food.calories * item.rawWeight / 100), 0);
+                const totalRawProtein = recipeIngredients.reduce((sum, item) => sum + (item.food.protein * item.rawWeight / 100), 0);
+                const totalRawFat = recipeIngredients.reduce((sum, item) => sum + (item.food.fat * item.rawWeight / 100), 0);
+                const totalRawCarbs = recipeIngredients.reduce((sum, item) => sum + (item.food.carbs * item.rawWeight / 100), 0);
+                
+                const cookedWeight = parseFloat(recipeCookedWeight) || totalRawWeight;
+                
+                const cookedCalories = cookedWeight > 0 ? Math.round((totalRawCalories / cookedWeight) * 100) : 0;
+                const cookedProtein = cookedWeight > 0 ? Math.round((totalRawProtein / cookedWeight) * 100 * 10) / 10 : 0;
+                const cookedFat = cookedWeight > 0 ? Math.round((totalRawFat / cookedWeight) * 100 * 10) / 10 : 0;
+                const cookedCarbs = cookedWeight > 0 ? Math.round((totalRawCarbs / cookedWeight) * 100 * 10) / 10 : 0;
+
+                return (
+                  <div style={{ textAlign: 'left', background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.06)', padding: '14px', borderRadius: '16px', display: 'flex', flexDirection: 'column', gap: '12px' }}>
+                    <div style={{ display: 'flex', justifyContent: 'space-between', fontSize: '12px', borderBottom: '1px solid rgba(255,255,255,0.05)', paddingBottom: '6px' }}>
+                      <span>Сира вага інгредієнтів:</span>
+                      <strong>{totalRawWeight} г</strong>
+                    </div>
+
+                    <div className="form-group">
+                      <label className="modal-label" style={{ fontWeight: 600, fontSize: '12px', display: 'block', marginBottom: '6px' }}>Вага готової страви після уварювання (г):</label>
+                      <input 
+                        type="number" 
+                        className="modal-input" 
+                        placeholder={totalRawWeight.toString()} 
+                        value={recipeCookedWeight} 
+                        onChange={(e) => setRecipeCookedWeight(e.target.value)} 
+                        style={{ width: '100%', boxSizing: 'border-box' }}
+                      />
+                      <span style={{ fontSize: '10px', color: '#94a3b8', display: 'block', marginTop: '4px', lineHeight: '1.3' }}>* Вода випаровується, тому вага готової страви зазвичай менша, ніж сирих інгредієнтів.</span>
+                    </div>
+
+                    <div style={{ borderTop: '1px dashed rgba(255,255,255,0.08)', paddingTop: '10px' }}>
+                      <span style={{ fontSize: '12px', fontWeight: 600, display: 'block', marginBottom: '8px', color: '#10b981' }}>Розраховано КБЖВ готової страви (на 100г):</span>
+                      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: '8px', textAlign: 'center' }}>
+                        <div style={{ background: 'rgba(255,255,255,0.02)', padding: '6px', borderRadius: '10px' }}>
+                          <span style={{ fontSize: '9px', opacity: 0.5, display: 'block' }}>Калорії</span>
+                          <strong style={{ fontSize: '13px', color: 'var(--color-calories)' }}>{cookedCalories}</strong>
+                        </div>
+                        <div style={{ background: 'rgba(255,255,255,0.02)', padding: '6px', borderRadius: '10px' }}>
+                          <span style={{ fontSize: '9px', opacity: 0.5, display: 'block' }}>Білки</span>
+                          <strong style={{ fontSize: '13px', color: 'var(--color-protein)' }}>{cookedProtein}г</strong>
+                        </div>
+                        <div style={{ background: 'rgba(255,255,255,0.02)', padding: '6px', borderRadius: '10px' }}>
+                          <span style={{ fontSize: '9px', opacity: 0.5, display: 'block' }}>Жири</span>
+                          <strong style={{ fontSize: '13px', color: 'var(--color-fat)' }}>{cookedFat}г</strong>
+                        </div>
+                        <div style={{ background: 'rgba(255,255,255,0.02)', padding: '6px', borderRadius: '10px' }}>
+                          <span style={{ fontSize: '9px', opacity: 0.5, display: 'block' }}>Вугл.</span>
+                          <strong style={{ fontSize: '13px', color: 'var(--color-carbs)' }}>{cookedCarbs}г</strong>
+                        </div>
+                      </div>
+                    </div>
+                  </div>
+                );
+              })()}
+            </div>
+            <div className="modal-footer" style={{ display: 'flex', gap: '10px', marginTop: '20px', justifyContent: 'flex-end' }}>
+              <button className="btn-secondary" onClick={() => setIsRecipeCreatorOpen(false)} style={{ width: 'auto', padding: '10px 18px' }}>Скасувати</button>
+              <button className="btn-primary" onClick={handleSaveRecipe} disabled={recipeIngredients.length === 0} style={{ width: 'auto', padding: '10px 18px' }}>Зберегти рецепт</button>
+            </div>
+          </div>
+        </div>
+      )}
+
       {/* Toast Notification Container */}
       {toast && (
         <div className={`toast-notification toast-${toast.type}`}>
