diff --git a/package-lock.json b/package-lock.json
index 35ad892..34c9963 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -8,6 +8,7 @@
       "name": "blissful-lavoisier",
       "version": "0.0.0",
       "dependencies": {
+        "html5-qrcode": "^2.3.8",
         "lucide-react": "^1.16.0",
         "react": "^19.2.6",
         "react-dom": "^19.2.6"
@@ -1510,6 +1511,12 @@
         "hermes-estree": "0.25.1"
       }
     },
+    "node_modules/html5-qrcode": {
+      "version": "2.3.8",
+      "resolved": "https://registry.npmjs.org/html5-qrcode/-/html5-qrcode-2.3.8.tgz",
+      "integrity": "sha512-jsr4vafJhwoLVEDW3n1KvPnCCXWaQfRng0/EEYk1vNcQGcG/htAdhJX0be8YyqMoSz7+hZvOZSTAepsabiuhiQ==",
+      "license": "Apache-2.0"
+    },
     "node_modules/ignore": {
       "version": "5.3.2",
       "resolved": "https://registry.npmjs.org/ignore/-/ignore-5.3.2.tgz",
diff --git a/package.json b/package.json
index 68c6dc1..e909ab4 100644
--- a/package.json
+++ b/package.json
@@ -10,6 +10,7 @@
     "preview": "vite preview"
   },
   "dependencies": {
+    "html5-qrcode": "^2.3.8",
     "lucide-react": "^1.16.0",
     "react": "^19.2.6",
     "react-dom": "^19.2.6"
diff --git a/public/sw.js b/public/sw.js
index 4f0dc96..57a3a85 100644
--- a/public/sw.js
+++ b/public/sw.js
@@ -1,4 +1,4 @@
-const CACHE_NAME = 'nutrisnap-cache-v33';
+const CACHE_NAME = 'nutrisnap-cache-v41';
 const ASSETS_TO_CACHE = [
   '/',
   '/index.html',
diff --git a/src/App.jsx b/src/App.jsx
index 9c13349..277551e 100644
--- a/src/App.jsx
+++ b/src/App.jsx
@@ -917,31 +917,46 @@ export default function App() {
     }
 
     let intervalId = null;
-    try {
-      const formats = ['ean_13', 'ean_8', 'upc_a', 'upc_e', 'code_128', 'code_39'];
-      const detector = new window.BarcodeDetector({ formats });
+    let isActive = true;
+
+    const initDetector = async () => {
+      try {
+        const supportedFormats = await window.BarcodeDetector.getSupportedFormats();
+        const desiredFormats = ['ean_13', 'ean_8', 'upc_a', 'upc_e', 'code_128', 'code_39', 'qr_code'];
+        const formats = desiredFormats.filter(f => supportedFormats.includes(f));
+        
+        if (formats.length === 0 || !isActive) {
+          console.log("No supported barcode formats found on this device.");
+          return;
+        }
 
-      intervalId = setInterval(async () => {
-        const video = videoRef.current;
-        if (!video || video.readyState !== 4) return;
+        const detector = new window.BarcodeDetector({ formats });
 
-        try {
-          const barcodes = await detector.detect(video);
-          if (barcodes && barcodes.length > 0) {
-            const code = barcodes[0].rawValue;
-            console.log("Natively detected barcode:", code);
-            clearInterval(intervalId);
-            triggerBarcodeSearchDirect(code);
+        intervalId = setInterval(async () => {
+          const video = videoRef.current;
+          if (!video || video.readyState !== 4 || !isActive) return;
+
+          try {
+            const barcodes = await detector.detect(video);
+            if (barcodes && barcodes.length > 0 && isActive) {
+              const code = barcodes[0].rawValue;
+              console.log("Natively detected barcode:", code);
+              clearInterval(intervalId);
+              triggerBarcodeSearchDirect(code);
+            }
+          } catch (detectErr) {
+            console.error("Barcode detection loop error:", detectErr);
           }
-        } catch (detectErr) {
-          console.error("Barcode detection loop error:", detectErr);
-        }
-      }, 400);
-    } catch (e) {
-      console.error("Failed to initialize BarcodeDetector:", e);
-    }
+        }, 400);
+      } catch (e) {
+        console.error("Failed to initialize BarcodeDetector:", e);
+      }
+    };
+
+    initDetector();
 
     return () => {
+      isActive = false;
       if (intervalId) clearInterval(intervalId);
     };
   }, [activeTab, scannerMode, cameraActive, barcodeResult, barcodeLoading, isBarcodeScanning, isNativeScannerSupported]);
diff --git a/src/index.css b/src/index.css
index eb0a2db..8260576 100644
--- a/src/index.css
+++ b/src/index.css
@@ -115,6 +115,12 @@ body.light-mode .app-container {
     border-radius: 0;
     border: none;
   }
+  .bottom-nav {
+    position: fixed;
+  }
+  .scan-fab {
+    position: fixed;
+  }
 }
 
 /* App Header styling */
@@ -431,11 +437,20 @@ body.light-mode .bottom-nav {
   align-items: center;
   gap: 4px;
   cursor: pointer;
-  padding: 8px 12px;
+  padding: 8px 4px;
   border-radius: 16px;
   font-size: 11px;
   font-weight: 500;
   transition: var(--transition-smooth);
+  flex: 1;
+  min-width: 0;
+}
+
+.nav-item span {
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  max-width: 100%;
 }
 
 body.light-mode .nav-item {
@@ -992,6 +1007,40 @@ body.light-mode .section-title {
   color: var(--text-light-primary);
 }
 
+.btn-back-header {
+  background: rgba(255, 255, 255, 0.08);
+  border: none;
+  color: white;
+  border-radius: 50%;
+  width: 32px;
+  height: 32px;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  cursor: pointer;
+  transition: all 0.2s ease;
+  padding: 0;
+  flex-shrink: 0;
+}
+
+.btn-back-header:hover {
+  background: rgba(255, 255, 255, 0.15);
+  transform: scale(1.05);
+}
+
+.btn-back-header:active {
+  transform: scale(0.95);
+}
+
+body.light-mode .btn-back-header {
+  background: rgba(15, 23, 42, 0.05);
+  color: var(--text-light-primary);
+}
+
+body.light-mode .btn-back-header:hover {
+  background: rgba(15, 23, 42, 0.1);
+}
+
 .diary-day-header {
   display: flex;
   justify-content: space-between;
@@ -2453,3 +2502,265 @@ body.light-mode .autocomplete-item:hover {
   background-color: rgba(0, 0, 0, 0.04);
 }
 
+/* Custom styling to make html5-qrcode video cover the preview container */
+#barcode-reader-container video {
+  width: 100% !important;
+  height: 100% !important;
+  object-fit: cover !important;
+}
+
+#barcode-reader-container {
+  width: 100%;
+  height: 100%;
+  position: absolute;
+  top: 0;
+  left: 0;
+}
+
+/* Global Modal & Overlay Styles */
+.modal-backdrop {
+  position: fixed;
+  top: 0;
+  left: 0;
+  width: 100vw;
+  height: 100vh;
+  background: rgba(4, 6, 12, 0.7);
+  backdrop-filter: blur(12px) saturate(180%);
+  -webkit-backdrop-filter: blur(12px) saturate(180%);
+  z-index: 500;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  padding: 16px;
+  box-sizing: border-box;
+  animation: modal-fade-in 0.25s cubic-bezier(0.16, 1, 0.3, 1);
+}
+
+.modal-content.glassmorphic-modal {
+  background: rgba(20, 26, 42, 0.75);
+  border: 1px solid rgba(255, 255, 255, 0.08);
+  border-radius: 28px;
+  width: 100%;
+  max-width: 440px;
+  max-height: 90vh;
+  box-shadow: var(--shadow-glass);
+  backdrop-filter: blur(25px);
+  -webkit-backdrop-filter: blur(25px);
+  color: white;
+  padding: 24px;
+  box-sizing: border-box;
+  display: flex;
+  flex-direction: column;
+  animation: modal-zoom-in 0.35s cubic-bezier(0.34, 1.56, 0.64, 1);
+  overflow: hidden;
+}
+
+body.light-mode .modal-content.glassmorphic-modal {
+  background: rgba(255, 255, 255, 0.85);
+  border: 1px solid rgba(0, 0, 0, 0.06);
+  color: var(--text-light-primary);
+  box-shadow: 0 10px 40px -10px rgba(0, 0, 0, 0.1);
+}
+
+@keyframes modal-fade-in {
+  from { opacity: 0; }
+  to { opacity: 1; }
+}
+
+@keyframes modal-zoom-in {
+  from { transform: scale(0.9) translateY(20px); opacity: 0; }
+  to { transform: scale(1) translateY(0); opacity: 1; }
+}
+
+.modal-header {
+  display: flex;
+  justify-content: space-between;
+  align-items: center;
+  margin-bottom: 20px;
+  flex-shrink: 0;
+}
+
+.modal-header h3 {
+  font-size: 18px;
+  font-weight: 700;
+  font-family: var(--font-display);
+  margin: 0;
+}
+
+.modal-close-btn {
+  background: rgba(255, 255, 255, 0.06);
+  border: none;
+  color: var(--text-dark-secondary);
+  border-radius: 50%;
+  width: 32px;
+  height: 32px;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  cursor: pointer;
+  transition: all 0.2s ease;
+}
+
+body.light-mode .modal-close-btn {
+  background: rgba(0, 0, 0, 0.04);
+  color: var(--text-light-secondary);
+}
+
+.modal-close-btn:hover {
+  background: rgba(255, 255, 255, 0.12);
+  color: white;
+  transform: rotate(90deg);
+}
+
+body.light-mode .modal-close-btn:hover {
+  background: rgba(0, 0, 0, 0.08);
+  color: var(--text-light-primary);
+}
+
+.modal-body {
+  flex-grow: 1;
+  overflow-y: auto;
+  padding-right: 4px;
+}
+
+.modal-footer {
+  display: flex;
+  gap: 12px;
+  margin-top: 24px;
+  justify-content: flex-end;
+  flex-shrink: 0;
+}
+
+.modal-label {
+  display: block;
+  font-size: 13px;
+  font-weight: 600;
+  color: var(--text-dark-secondary);
+  margin-bottom: 8px;
+  text-align: left;
+}
+
+body.light-mode .modal-label {
+  color: var(--text-light-secondary);
+}
+
+.modal-input {
+  width: 100%;
+  background: rgba(255, 255, 255, 0.03);
+  border: 1px solid rgba(255, 255, 255, 0.08);
+  border-radius: 12px;
+  padding: 12px 14px;
+  color: white;
+  font-size: 14px;
+  outline: none;
+  box-sizing: border-box;
+  transition: all 0.2s ease;
+}
+
+body.light-mode .modal-input {
+  background: rgba(0, 0, 0, 0.02);
+  border: 1px solid rgba(0, 0, 0, 0.08);
+  color: var(--text-light-primary);
+}
+
+.modal-input:focus {
+  background: rgba(255, 255, 255, 0.06);
+  border-color: var(--color-calories);
+  box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.15);
+}
+
+body.light-mode .modal-input:focus {
+  background: #ffffff;
+  border-color: var(--color-calories);
+  box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.1);
+}
+
+/* Animated Water Wave Effects */
+.water-wave-front {
+  animation: wave-slide-front 12s linear infinite;
+  transform-origin: center bottom;
+}
+
+.water-wave-back {
+  animation: wave-slide-back 8s linear infinite;
+  transform-origin: center bottom;
+}
+
+@keyframes wave-slide-front {
+  0% {
+    transform: translateX(-40%) rotate(0deg);
+  }
+  50% {
+    transform: translateX(-10%) rotate(180deg);
+  }
+  100% {
+    transform: translateX(-40%) rotate(360deg);
+  }
+}
+
+@keyframes wave-slide-back {
+  0% {
+    transform: translateX(-20%) rotate(0deg);
+  }
+  50% {
+    transform: translateX(-50%) rotate(180deg);
+  }
+  100% {
+    transform: translateX(-20%) rotate(360deg);
+  }
+}
+
+/* SVG Chart styles */
+.weight-chart-container {
+  border-radius: 16px;
+  padding: 8px;
+  background: rgba(255, 255, 255, 0.01);
+}
+
+.weight-chart-line {
+  stroke-dasharray: 1000;
+  stroke-dashoffset: 1000;
+  animation: draw-chart-line 2s cubic-bezier(0.4, 0, 0.2, 1) forwards;
+}
+
+.weight-chart-area {
+  opacity: 0;
+  animation: fade-in-chart-area 1.5s ease-out 1s forwards;
+}
+
+.weight-chart-point {
+  opacity: 0;
+  animation: pop-chart-point 0.5s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
+}
+
+.weight-chart-point:nth-child(1) { animation-delay: 1.2s; }
+.weight-chart-point:nth-child(2) { animation-delay: 1.3s; }
+.weight-chart-point:nth-child(3) { animation-delay: 1.4s; }
+.weight-chart-point:nth-child(4) { animation-delay: 1.5s; }
+.weight-chart-point:nth-child(5) { animation-delay: 1.6s; }
+.weight-chart-point:nth-child(6) { animation-delay: 1.7s; }
+.weight-chart-point:nth-child(7) { animation-delay: 1.8s; }
+
+@keyframes draw-chart-line {
+  to {
+    stroke-dashoffset: 0;
+  }
+}
+
+@keyframes fade-in-chart-area {
+  to {
+    opacity: 1;
+  }
+}
+
+@keyframes pop-chart-point {
+  from {
+    transform: scale(0);
+    opacity: 0;
+  }
+  to {
+    transform: scale(1);
+    opacity: 1;
+  }
+}
+
diff --git a/src/services/geminiService.js b/src/services/geminiService.js
index 9365b66..dfabfc8 100644
--- a/src/services/geminiService.js
+++ b/src/services/geminiService.js
@@ -241,3 +241,81 @@ export async function estimateFoodNutritionByName(foodName, apiKey, modelName =
     throw error;
   }
 }
+
+export async function analyzeProductPackagingImage(base64Data, apiKey, modelName = 'gemini-2.5-flash') {
+  if (!apiKey) {
+    throw new Error("API-ключ не налаштовано. Будь ласка, введіть ваш Gemini API-ключ у налаштуваннях.");
+  }
+  const trimmedKey = apiKey.trim();
+  const base64ImageBytes = base64Data.replace(/^data:image\/\w+;base64,/, "");
+  const url = `https://generativelanguage.googleapis.com/v1beta/models/${modelName}:generateContent?key=${trimmedKey}`;
+
+  const promptText = `
+    Проаналізуй це зображення упаковки продукту харчування (зокрема таблицю харчової цінності або опис КБЖВ).
+    Твоє завдання:
+    1. Визначити назву продукту та бренд (наприклад, "Agrola - Хліб зерновий").
+    2. Знайти калорійність (ккал), білки (г), жири (г) та вуглеводи (г) НА 100г продукту.
+    3. Знайти загальну вагу упаковки або типову порцію в грамах (якщо вказано, інакше поверни 100).
+    4. Зчитати склад/інгредієнти одним реченням.
+    
+    Ти ПОВИНЕН повернути відповідь виключно у форматі JSON українською мовою з наступними полями:
+    - "name": Назва продукту разом з брендом
+    - "calories": Калорійність на 100г у ккал (ціле число)
+    - "protein": Білки на 100г в грамах (число, огруглене до 1 знака)
+    - "fat": Жири на 100г в грамах (число, огруглене до 1 знака)
+    - "carbs": Вуглеводи на 100г в грамах (число, огруглене до 1 знака)
+    - "weight": Загальна вага упаковки або порції в грамах (ціле число, за замовчуванням 100)
+    - "ingredients": Склад продукту (одним реченням, або null якщо не вказано)
+    
+    Формат відповіді має бути чистим JSON об'єктом, без markdown розмітки на кшталт \`\`\`json.
+  `;
+
+  const payload = {
+    contents: [
+      {
+        parts: [
+          { text: promptText },
+          {
+            inlineData: {
+              mimeType: "image/jpeg",
+              data: base64ImageBytes
+            }
+          }
+        ]
+      }
+    ],
+    generationConfig: {
+      responseMimeType: "application/json"
+    }
+  };
+
+  try {
+    const response = await fetch(url, {
+      method: "POST",
+      headers: {
+        "Content-Type": "application/json"
+      },
+      body: JSON.stringify(payload)
+    });
+
+    if (!response.ok) {
+      const errorData = await response.json().catch(() => ({}));
+      throw handleGeminiError(response, errorData);
+    }
+
+    const data = await response.json();
+    const textResponse = data.candidates?.[0]?.content?.parts?.[0]?.text;
+    if (!textResponse) {
+      throw new Error("ШІ не зміг згенерувати відповідь для цього зображення.");
+    }
+
+    const parsedData = JSON.parse(textResponse);
+    return parsedData;
+  } catch (error) {
+    console.error("Error in analyzeProductPackagingImage:", error);
+    if (error instanceof SyntaxError) {
+      throw new Error("Не вдалося розпарсити відповідь від ШІ. Спробуйте ще раз.");
+    }
+    throw error;
+  }
+}
diff --git a/src/services/openFoodFactsService.js b/src/services/openFoodFactsService.js
index d1d79c5..a0a856b 100644
--- a/src/services/openFoodFactsService.js
+++ b/src/services/openFoodFactsService.js
@@ -27,6 +27,10 @@ export async function getProductByBarcode(barcode) {
   
   const response = await fetch(url);
 
+  if (response.status === 404) {
+    throw new Error("Продукт із таким штрих-кодом не знайдено у базі даних.");
+  }
+
   if (!response.ok) {
     throw new Error("Не вдалося підключитися до бази даних продуктів. Спробуйте пізніше.");
   }
